# DORON'S RAVE — כל החישובים במערכת
**עודכן:** 2026-09-20 · גרסה @23

מסמך ייחוס. כל מספר שהאפליקציה מציגה מופיע כאן עם הנוסחה המדויקת ומיקומה בקוד.
**חולץ מהקוד עצמו, לא מהזיכרון.** כל 44 הבדיקות ב-`test-eventblocks.js` נועלות את מה שכתוב כאן.

כלל-על: **אין מספר בלי מקור.** אם מספר לא נגזר משורה בגיליון — הוא לא מוצג.

---

## 1. יסודות

| מושג | נוסחה | מקור |
|---|---|---|
| **שולם (תשלום בודד)** | `payment_status === 'שולם'` | `isPaymentPaid_` · PaymentsService |
| **באיחור** | לא שולם **וגם** יש `due_date` **וגם** `due_date < היום` | `isPaymentOverdue_` |
| **סכום הכנסה** | `כמות × מחיר ליחידה`, מעוגל לאגורה | `saveIncome` · IncomeService |

> התאריכים נשמרים כ-`yyyy-MM-dd`, ולכן השוואת מחרוזות שקולה להשוואת תאריכים.

---

## 2. הוצאה בודדת

| מושג | נוסחה | מקור |
|---|---|---|
| **שולם** | סכום התשלומים של ההוצאה שסומנו "שולם" | `summarizeExpensePayments_` |
| **נותר** | `סוכם − שולם` | " |
| **סטטוס מחושב** | שולם = 0 → הסטטוס הידני · 0 < שולם < סוכם → **שולם חלקית** · שולם ≥ סוכם → **שולם** | " |
| **מועד הבא** | התאריך המוקדם ביותר מבין התשלומים **שטרם שולמו** | " |
| **יש איחור** | לפחות תשלום אחד באיחור | " |

**הסטטוס הידני אף פעם לא נדרס בשרת.** ברירת המחדל היא "מתוכנן"; תיקון סכומים לפי סטטוס
שייך לטופס בלבד (החלטה 51), אחרת ייבוא או שכפול היו מאבדים את `agreed_amount`.

---

## 3. סיכומי אירוע — `computeEventTotals_`

| מושג | נוסחה |
|---|---|
| **הוצאות (מוסכם)** | סכום `agreed_amount` של כל הוצאות האירוע הפעילות |
| **הכנסות** | סכום `total_amount` של כל שורות ההכנסה |
| **מאזן** | `הכנסות − הוצאות` |
| **שולם** | סכום התשלומים ששולמו, **רק** של הוצאות פעילות של האירוע |
| **נותר לתשלום** | `הוצאות − שולם` |

⚠ **אמן הוא סוג הוצאה, לא ישות כספית מקבילה** (החלטה 6). לכן הוא נספר פעם אחת בלבד
ואין Double Counting. **הוצאה שאורכבה יוצאת מכל הסיכומים**, והשורה נשארת בגיליון.

---

## 4. תמונה פיננסית — `computeEventPnl_`

| מושג | נוסחה |
|---|---|
| **הוצאות מתוכננות** | לכל הוצאה **בנפרד**: `planned_amount > 0 ? planned_amount : agreed_amount`, ואז סכום |
| **כרטיסים שנמכרו** | סכום `quantity` של שורות הכנסה שסומנו `is_ticket_income` |
| **הכנסות מכרטיסים** | סכום `total_amount` של אותן שורות |
| **הכנסות שאינן כרטיסים** | סכום `total_amount` של כל השאר |
| **מחיר כרטיס ממוצע בפועל** | `הכנסות מכרטיסים ÷ כרטיסים שנמכרו` (null אם לא נמכרו) |
| **מחיר לחישוב** | קודם `Events.average_ticket_price` · אחרת הממוצע בפועל · אחרת אין (החלטה 24) |
| **עלות שנותרה לכסות** | `max(0, מתוכנן − הכנסות שאינן כרטיסים)` |
| **נקודת איזון (כרטיסים)** | `⌈עלות שנותרה ÷ מחיר לחישוב⌉` · null בלי מחיר |
| **כרטיסים חסרים לאיזון** | `max(0, נקודת איזון − כרטיסים שנמכרו)` — **לעולם לא שלילי** |
| **הכנסה צפויה מכרטיסים** | `expected_ticket_count × מחיר לחישוב` |
| **רווח צפוי** | `(הכנסה צפויה מכרטיסים + הכנסות שאינן כרטיסים) − מתוכנן` |

**"רווח צפוי" קיים רק כששניהם הוזנו** — מחיר כרטיס וכמות צפויה. אחרת הוא null,
והממשק מציג את היתרה בפועל במקומו ולא מספר מומצא.
**מספר שלילי מוצג כ"הפסד צפוי" עם ערך חיובי** (החלטה 75).

### 4א. סבבי תמחור (2026-09-20)

`Events.ticket_tiers` מחזיקה רשימת סבבים כ-JSON: `[{name, quantity, price}]`,
**לפי סדר המכירה**. למשל: מוקדם 100×150 · רגיל 150×200 · VIP 50×400.

| מושג | נוסחה |
|---|---|
| **נקודת איזון** | מוכרים **לפי הסדר**: ממלאים את הסבב הראשון, וכשהוא נגמר עוברים לבא, עד שהעלות מכוסה |
| **לא ניתן לאיזון** | כל הסבבים יחד לא מכסים את העלות → `break_even_tickets = null` ו-`break_even_unreachable = true`. **לא מוצג כ-0** |
| **הכנסה צפויה מכרטיסים** | `Σ (כמות × מחיר)` על כל הסבבים |
| **כמות צפויה** | סכום הכמויות בכל הסבבים |

**כשיש סבבים הם גוברים** על `average_ticket_price` ו-`expected_ticket_count`.
**בלי סבבים החישוב נשאר בדיוק כפי שהיה.** נעול ב-`test-tiers.js`.

> **מלכודת שכדאי להכיר:** `average_ticket_price` גובר על הממוצע בפועל.
> אם מכרת בממוצע 230 אבל כתוב באירוע 200 — נקודת האיזון תחושב לפי 200.
> מי שמוכר בכמה מחירים עדיף שישתמש בסבבים, או שישאיר את השדה ריק.

---

## 5. מצב ההפקה — `buildEventReadiness_` · `summarizeEventReadiness_`

**הרשימה היא של המשתמשת.** `Events.required_items` מחזיקה את מה שהיא סימנה שהאירוע צריך,
כמפתחות `sub:<id>` / `cat:<id>`. ריק = לא הוגדר.

> עד 2026-09-20 הרשימה הייתה 11 שלבים קבועים ש-Claude המציא. היא בוטלה.

**מצב של כל רכיב** — נגזר מההוצאות, אף פעם לא מוקלד:

| מצב | מתי |
|---|---|
| **מכוסה** | יש הוצאה בתחום עם `agreed_amount > 0` **וגם** (סטטוס "סוכם" **או** כבר שולם עליה משהו) |
| **כלול ב-X** | התחום מסומן כרכיב בעסקה כוללת אחרת (`ExpenseCoverage`) |
| **בתהליך** | יש הוצאה בתחום אבל היא עוד לא נסגרה |
| **חסר** | אין כלום |

**עדיפות:** מכוסה ← כלול ← בתהליך. **ההתאמה לפי מזהה**, ולכן שינוי שם של קטגוריה לא שובר.

| מושג | נוסחה |
|---|---|
| **סגורים** | מספר הרכיבים במצב מכוסה או כלול |
| **חסרים** | מספר הרכיבים במצב חסר |
| **אחוז מוכנות** | `round(סגורים ÷ סה"כ שנבחרו × 100)` |
| **הוגדר?** | `נבחר לפחות רכיב אחד` |

**אירוע שלא הוגדר לא מציג אחוז — גם לא 0%.** אין מכנה, אין מספר.
**בחירת רכיבים לא משנה שום סכום.** נעול בבדיקה.

---

## 6. לוח האמנים — `buildArtistLineup_`

נכנסות רק הוצאות אמנים שיש להן **גם** שעת התחלה **וגם** שעת סיום.

| מושג | נוסחה |
|---|---|
| **משך ההופעה** | `סיום − התחלה` · הפרש **שלילי** = חציית חצות, מוסיפים 24 שעות |
| **שעות זהות** | **נחסם בשמירה.** 22:00–22:00 אינו הופעה של 24 שעות |
| **עלות לשעה** | `סוכם ÷ (משך בדקות ÷ 60)`, מעוגל — תצוגה בלבד, לא נשמר |
| **מיון** | לפי "רצף לילה": שעה לפני 12:00 נחשבת ללילה שאחרי (21:00 < 23:30 < 00:30 < 02:00) |
| **חפיפה** | שני מקטעים נחתכים בפועל. **נגיעה בקצה אינה חפיפה** (22:00–23:00 מול 23:00–01:00) |

**חפיפה היא אזהרה בלבד ואינה חוסמת שמירה** (החלטה 28).

⚠ **מספר האמנים על כרטיס האירוע נספר מההוצאות (`artist_id`), לא מהליינאפ** —
אחרת אירוע עם שישה אמנים בלי שעות היה מציג "0 אמנים".

---

## 7. חלוקת הוצאות

| חלוקה | נוסחה |
|---|---|
| **לפי קטגוריה** | סכום `agreed_amount` לפי `category_name` |
| **לפי תת-קטגוריה** | בתוך כל קטגוריה, לפי `subcategory_name` · ריק = "ללא תת-קטגוריה" |
| **לפי ספק** | סכום `agreed_amount` לפי `vendor_name` · **הוצאה בלי ספק נאספת ל"ללא ספק"** |

**סכום תתי-הקטגוריות = סכום הקטגוריה, וסכום הקטגוריות = סה"כ ההוצאות.**
כל הוצאה נספרת **פעם אחת בלבד**. "אמנים" לא מקבלת פירוט תתי-קטגוריות.

**גם החלוקה לפי ספק מסתכמת לסה"כ** (תוקן 2026-09-20). הוצאה בלי ספק — למשל הוצאת
אמן — נאספת לשורת "ללא ספק" במקום להישמט, כדי ששקלים לא ייעלמו מהחלוקה.

---

## 8. המספרים בבית — `summarizeActiveFinances_`

מחושבים על **האירועים הפעילים בלבד** (עתידיים).

| מושג | נוסחה |
|---|---|
| **מתוכנן** | לכל הוצאה: `planned_amount > 0 ? planned_amount : agreed_amount`, סכום |
| **מוסכם** | סכום `agreed_amount` |
| **שולם** | סכום התשלומים ששולמו של אותן הוצאות |
| **נותר לתשלום** | `מוסכם − שולם` |
| **הכנסות** | סכום `total_amount` |
| **רווח צפוי** | סכום ה"רווח צפוי" של כל אירוע · **אם לאירוע אין תחזית** — נופל ל-`(הכנסות בפועל) − מתוכנן` |
| **תשלומים קרובים** | תשלומים שטרם שולמו, עם `due_date`, של אירועים פעילים — ממוינים לפי מועד |
| **באיחור** | מתוכם, אלה שהמועד שלהם עבר |

`forecast_is_projection` מסמן אם לפחות אירוע אחד תרם תחזית אמיתית —
הממשק כותב "לפי תחזית הכרטיסים" או "לפי ההכנסות בפועל" בהתאם.

---

## 9. חלונות זמן

| קבוע | ערך | למה |
|---|---|---|
| `ATTENTION_HORIZON_DAYS` | 7 | תשלום נחשב "השבוע" |
| `EVENT_READINESS_HORIZON_DAYS` | 30 | אירוע נחשב "מתקרב" |

---

## 10. מה **לא** מחושב — פערים ידועים

- **"ספקים ואנשי קשר" אינו סיכום כספי.** הוא נגזר מההוצאות שיש להן ספק, ולכן
  **הוצאה בלי ספק לא מופיעה בו**. זה בלוק של אנשים, לא של כסף. נעול בבדיקה.
  (בניגוד ל**חלוקה** לפי ספק, שכן מסתכמת לסה"כ דרך "ללא ספק".)
- **`getEvents` לא מחזיר `readiness`** — רק `pnl` ו-`artists_count`. לכן אחוז המוכנות
  מופיע בבית (מ-`getInitialData`) ולא ברשימת האירועים.
- **`computeEventTotals_` לא משתנה לעולם.** כל תמונה מורחבת נמצאת ב-`computeEventPnl_`
  בלבד (החלטה 22). אין לשנות את משמעות השדות שלו.
