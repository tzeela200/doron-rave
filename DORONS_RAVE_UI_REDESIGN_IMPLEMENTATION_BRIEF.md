# DORON'S RAVE — חבילת הטמעה לעיצוב מחדש

## מטרת המסמך

מסמך זה הוא הוראת עבודה מחייבת ליישום עיצוב מחדש מלא של שכבת הממשק (Frontend UI) של DORON'S RAVE.

המטרה היא **לא לצמצם את המערכת** ולא לשנות את העסק או את הנתונים. המטרה היא לארגן מחדש את כל מה שקיים למוצר מקצועי לניהול והפקת אירועים: נוח, ברור, עשיר, נגיש, RTL ומותאם קודם כול לנייד.

המערכת היא לא רק כלי תקציבי. היא מערכת לניהול אירוע מקצה לקצה: אירועים, תחומי הפקה, קטגוריות ותתי-קטגוריות, אמנים, ספקים, התחייבויות/הוצאות, תשלומים ומקדמות, הכנסות, תחזיות, P&L, כרטיסים, מוכנות, כיסוי, הערות, תזכורות, חיפוש, השוואה והגדרות.

---

## חוק עליון

כל מסך חייב לענות בתוך 10 שניות על השאלה:

> אם הייתי מפיק/ת אירועים ופתחתי את המסך הזה עכשיו — מה אני חייב/ת לדעת, ומה אני יכול/ה לעשות מכאן?

כל יכולת קיימת נשארת. אין להסיר מידע, חישוב או פעולה כדי “לפשט”. במקום זאת, יש להשתמש בהיררכיית מידע ובחשיפה הדרגתית:

1. מצב קריטי / החלטה / פעולה עכשיו.
2. סיכום העבודה של המסך.
3. פירוט מקצועי מלא, כולל כל הנתונים הקיימים.

---

# 1. גבולות שינוי מחייבים

## מותר לשנות

רק את שכבת ה-UI ואת אופן הצגת המידע:

- `src/Index.html`
- `src/Stylesheet.html`
- `src/JavaScript.html`

מותר לשנות:

- HTML, CSS ו-renderers בצד הלקוח
- היררכיית מידע
- סדר אזורים במסכים
- ניווט ויזואלי בין מסכים קיימים
- קומפוננטות UI
- ניסוחי כותרות, עזרה ותוויות UI, כל עוד לא משנים ערכי נתונים או משמעות עסקית
- מבנה Bottom Sheets, רשימות, כרטיסים, אקורדיונים, כפתורים וסרגלי ניווט
- CSS, עיצוב, נגישות, RTL ורספונסיביות

## אסור לשנות

אין לגעת בקבצים או בהתנהגויות הבאים ללא אישור מפורש ונפרד:

- כל קובץ `.gs`
- `SHEET_HEADERS`, טבלאות, עמודות, שמות שדות או סכמת Google Sheets
- כל API או שם פונקציה של `google.script.run`
- חישובי מתוכנן / מוסכם / שולם / נותר
- חישובי הכנסות, P&L, תחזית, מחיר כרטיס ונקודת איזון
- תשלומים מפוצלים, מקדמות, תאריכי תשלום ואיחורים
- לוגיקת כיסוי (`Coverage`) או מניעת Double Counting
- לוגיקת מצב הפקה, דרישות, מוכנות ופריטי “לא צריך”
- לוגיקת אמנים, שעות הופעה, חציית חצות, משך, עלות לשעה וחפיפות
- לוגיקת שכפול, IDs חדשים והזזת תאריכים
- ארכוב (`is_archived`) או השבתת אמצעי תשלום (`is_active`)
- הרשאות, משתמשים, הגדרות פריסה או Deployment
- נתונים קיימים ב-Google Sheets

אין להריץ:

- `initializeSystem()`
- `upgradeSchema()`
- `seedCategoryTaxonomy()`
- ניקוי נתונים או כל פעולה בלתי הפיכה

אין להוסיף React, Tailwind, framework, build step, DB חדש או שירות חיצוני.

---

# 2. מפת המוצר

## ניווט תחתון

יש לשמור על חמש נקודות הגישה הקיימות, אך לשפר את התצוגה, התוויות וההתנהגות:

| אזור | תפקיד |
|---|---|
| בית | תמונת מצב רוחבית: מה דורש טיפול, אירועים פעילים, תשלומים ומדדים |
| אירועים | רשימה, חיפוש, סינון, יצירה וניהול של כל האירועים |
| + | יצירה מהירה בהקשר: אירוע, הוצאה, הכנסה, ספק, אמן או תזכורת לפי היכולות הקיימות |
| השוואה | השוואת אירועים ודוחות קיימים |
| הגדרות | קטגוריות, תתי-קטגוריות, ספקים, אמנים, אמצעי תשלום, ייבוא וכלי ניהול |

אין להסיר יכולת קיימת מהניווט. אם חלק מהיכולות נגישות גם מתוך אירוע — זה תוספת ניווט, לא החלפה.

## תיק האירוע הוא מרכז העבודה

מסך האירוע הוא סביבת העבודה העיקרית. הוא חייב להכיל את כל המידע הקיים, אבל מאורגן לפי חמש קבוצות עבודה:

1. מצב האירוע
2. הפקה וקטגוריות
3. אנשים וליינאפ
4. כספים
5. תיעוד וניהול

אין למחוק אקורדיונים או מסכים קיימים רק כי הם רבים. אפשר לארגן אותם מחדש תחת קבוצות-העל האלה, להוסיף סיכום לכותרת של כל אזור, ולהציג פירוט בהדרגה.

---

# 3. מבנה מסכים מחייב

## 3.1 מסך הבית

### מה חייב להיות ברור תוך 10 שניות

- מה דורש טיפול עכשיו.
- איזה אירוע קרוב או בסיכון.
- האם קיימים תשלומים באיחור או תשלומים קרובים.
- מה מצב העסק הכללי: אירועים פעילים, יתרה לתשלום, תחזית רווח ופריטי טיפול.

### סדר תוכן

1. לוגו קומפקטי בלבד; אינו דוחק מידע תפעולי.
2. כותרת/ברכה קצרה ללא שם אישי.
3. אזור “לטיפול עכשיו” עם משימות קיימות, לפי סדר הדחיפות שהשרת כבר מחזיר.
4. שורת KPI של ארבעת המדדים הקיימים. כל KPI לחיץ ומוביל ליעד הקיים שלו.
5. “אירועים פעילים” — כל האירועים העתידיים, לא רק הקרוב.
6. “תשלומים קרובים” — כולל איחורים.
7. “תמונה פיננסית” — החישובים הקיימים ללא שינוי.
8. “פעולות מהירות” — כל פעולות היצירה הקיימות.
9. “ניהול” — כל אפשרויות הניהול הקיימות, ברשימה מקופלת או בקבוצה ברורה.
10. היסטוריה, אם היא קיימת, באזור משני לאחר התפעול השוטף.

### כרטיס אירוע בבית

יש להציג בכל כרטיס/שורה:

- שם האירוע
- תאריך ומקום
- ימים עד האירוע
- מוכנות אמיתית אם קיימת
- מספר פריטי טיפול או חוסרים
- אינדיקציית תשלום/מצב כלכלי רלוונטית
- מעבר ברור לתיק האירוע

אין להסתמך על צבע בלבד; יש להציג טקסט קצר כגון “2 חוסרים”, “תשלום באיחור”, “נותרו 4,200 ₪”.

---

## 3.2 מסך אירועים

### מה חייב להיות ברור תוך 10 שניות

- אילו אירועים קרובים.
- איזה אירוע דורש טיפול.
- מה מצב ההפקה והכסף ברמת סיכום לכל אירוע.

### מבנה

- כותרת עם מונה תוצאות.
- חיפוש וסינון קיימים.
- מתג/סינון: עתידיים / עברו / ארכיון, בהתאם ליכולות קיימות.
- כפתור “אירוע חדש” ברור ונגיש.
- רשימה עשירה של אירועים.

כל אירוע חייב לשמור את פעולות העריכה, השכפול והארכוב. פעולות מסוכנות/משניות ייכנסו לתפריט שלוש נקודות, לא ייעלמו.

---

## 3.3 תיק אירוע

### שכבה 1: מצב על

החלק העליון מציג:

- שם האירוע
- תאריך
- מקום אם קיים
- ימים לאירוע כמספר מוביל
- סטטוס מוכנות אמיתי
- מצב כללי קצר: מספר חוסרים / פריטים בתהליך / תשלומים קרובים
- תפריט פעולות: עריכת אירוע, שכפול, ארכוב וכל פעולה קיימת

אין להציג סכום כספי ענק לפני שהמשתמש מבין איזה אירוע הוא רואה ומה מצב ההפקה שלו.

### שכבה 2: טיפול עכשיו

יש להציג את כל פריטי “דורש טיפול” הקיימים, אך בצורה של פעולות ברורות:

- פעולה
- הקשר: קטגוריה, ספק, אמן או אירוע
- סכום/מועד כאשר יש משמעות
- כניסה ישירה לרשומה הרלוונטית

יש להגביל את התצוגה הראשונית לרשימה סריקה; כל הפריטים נשארים זמינים דרך “הצג הכול”.

### שכבה 3: הפקה לפי קטגוריות

כל הקטגוריות ותתי-הקטגוריות נשארות. יש להציג אותן כתחומי הפקה:

- שם תחום
- מצב: מכוסה / כלול / בתהליך / חסר / לא צריך
- מספר התחייבויות/הוצאות
- אינדיקציית סכום קצרה
- פעולה להוספת הוצאה או פתיחת תחום

בלחיצה, נפתח פירוט התחום עם כל תתי-הקטגוריות, כל ההוצאות/התחייבויות, ספקים, אמנים, כיסוי, תשלומים ונתונים כספיים שקשורים לתחום.

### שכבה 4: אנשים וליינאפ

יש לשמר ולהציג:

- כל האמנים
- שם במה ושם אמיתי
- שעות התחלה וסיום
- משך
- עלות לשעה
- אזהרות חפיפה
- יתרה ותשלומים
- הערות ותזכורות לאמן
- ספקים, אנשי קשר, התחייבויות ויתרות

אמנים יכולים להישאר מודל חשבונאי מסוג הוצאה; אין לשנות זאת. התצוגה בלבד צריכה להיות “ליינאפ” תפעולי עשיר.

### שכבה 5: כספים

כל המידע והחישובים נשארים:

- מתוכנן
- מוסכם
- שולם
- נותר
- הכנסות
- תחזית רווח/הפסד
- תחזית כרטיסים
- מחיר כרטיס ממוצע
- נקודת איזון
- כרטיסים נדרשים לנקודת איזון
- תשלומים ומקדמות
- חלוקת הוצאות לפי קטגוריה ותת-קטגוריה
- חלוקת הוצאות לפי ספק

יש לתת לסיכום הכספי מבנה ברור: תחילה סיכום, אחר כך תשלומים, אחר כך הכנסות, ואז פירוט וחלוקה. אין לשנות שום נוסחה או מקור נתון.

### שכבה 6: תיעוד וניהול

יש לשמור:

- הערות
- תזכורות
- סטטוס תזכורות
- פעולות עריכה/שכפול/ארכוב
- היסטוריה אם קיימת

---

## 3.4 טופס הוצאה / התחייבות

כל השדות, הערכים וההתנהגויות נשארים. רק הסדר והצגת השדות משתנים.

### אזור א: פרטי ההתחייבות

- על מה ההוצאה
- קטגוריה
- תת-קטגוריה
- ספק או אמן
- קשר לאירוע

### אזור ב: מצב כספי

- סטטוס
- סכום מתוכנן
- סכום מוסכם
- סך שולם
- נותר
- מועד תשלום
- אמצעי תשלום
- תשלומים ומקדמות קיימים

חובה לשמור על לוגיקת “מצב פשוט” לעומת תשלומים מפוצלים. אין לשנות את החוזה הקיים: בטופס הראשי מועד תשלום מנהל תשלום יחיד בלבד במצב פשוט, ובפיצול הניהול נשאר באזור תשלומים ומקדמות.

### אזור ג: מה כלול

- מתג ה-Coverage הקיים
- כל רכיבי הכיסוי הקיימים
- התנהגות ארכוב/החייאה קיימת
- ללא Double Counting

האזור יהיה סגור/מוסתר עד שהמשתמשת בוחרת שהעסקה כוללת רכיבים נוספים, אך לעולם לא יוסר.

### אזור ד: פרטים נוספים

- תאריך
- הערות
- כל שדה משלים שכבר קיים
- ארכוב/מחיקה רק באזור נפרד ומוגן

כפתור שמירה חייב להישאר דביק ונגיש תמיד.

---

## 3.5 הכנסות, תשלומים והערות

אין להסיר שום שדה או פעולה.

### הכנסה

- שם/תיאור
- סכום, כמות, מחיר יחידה
- סימון הכנסות כרטיסים
- חישוב חי קיים
- עריכה וארכוב

### תשלום

- סכום
- תאריך/מועד
- אמצעי תשלום
- סטטוס
- חיבור להוצאה ולאירוע
- מקדמות ופיצולים
- עריכה/ארכוב לפי הקיים

### הערות ותזכורות

- קשר לאירוע או לאמן
- הערה/תזכורת
- תאריך
- סטטוס
- ארכוב במקום מחיקה

---

## 3.6 אמנים וספקים

יש לשמור את כל יכולות היצירה, העריכה, הארכוב והייבוא.

### כרטיס אדם

- שם תצוגה
- סוג: אמן/ספק
- טלפון ופרטי קשר
- מספר אירועים
- התחייבויות/סכומים רלוונטיים
- יתרה לתשלום או מצב פתוח
- הערות
- מעבר לפרטי האדם

### מסך אמן

- שם במה, שם אמיתי ושם תאימות קיים
- ליינאפ והופעות לפי אירוע
- שעות הופעה
- משך
- עלות לשעה
- חפיפות
- תשלומים והערות

### מסך ספק

- פרטי קשר
- תחומי הפקה
- אירועים משויכים
- הוצאות, תשלומים ויתרות
- הערות

---

## 3.7 קטגוריות ותתי-קטגוריות

כל היכולות נשארות: יצירה, עריכה, ארכוב, seed קיים, קשר לאמנים, וסינון תלוי-קטגוריה.

### התנהגות UX

- קטגוריות סגורות כברירת מחדל.
- פתיחת קטגוריה אחת בכל פעם כברירת מחדל.
- כפתור “פתח הכול” למשתמשת שרוצה סקירה מלאה.
- חיפוש קטגוריות ותתי-קטגוריות.
- כותרת קטגוריה מציגה מונה תתי-קטגוריות.
- פעולות עריכה וארכוב זמינות אך אינן יוצרות רעש על כל שורה.
- תתי-קטגוריות מובחנות חזותית מהקטגוריה ההורה.
- קטגוריית אמנים שומרת על ההתנהגות המיוחדת הקיימת: ללא תתי-קטגוריות, עם קישור לרשימת אמנים.

אין לשנות שמות, מזהים, סדר נתונים או לוגיקת ארכוב.

---

## 3.8 חיפוש, השוואה והגדרות

### חיפוש וסינון

כל 11 המסננים והצ׳יפים הפעילים נשארים. יש לשפר את חוויית ה-Bottom Sheet:

- כותרת דביקה
- איפוס ברור
- סיכום מספר תוצאות
- מסננים תלויים בקטגוריה כאשר זה קיים
- כפתור החל/שמירה דביק

### השוואת אירועים

הטבלה והגרף הקיימים נשארים. בנייד:

- טבלה נגללת אופקית בצורה מודעת ונגישה
- העמודה הראשונה נשארת דביקה
- ערכי 0 נשארים מוצגים לפי הלוגיקה הקיימת
- הגרף נשאר, אך לא דוחק את המידע המספרי

### הגדרות

כל אפשרויות הניהול נשארות זמינות, ברשימת הגדרות היררכית:

- קטגוריות
- ספקים
- אמנים
- אמצעי תשלום
- תשלומים קרובים
- חיפוש וסינון
- השוואת אירועים
- Google Sheets לפי היכולת הקיימת
- חשבון מחובר
- ייבוא

---

# 4. Design System מחייב

## כיוון ויזואלי

- מוצר מקצועי לניהול הפקת אירועים, לא “דשבורד גנרי” ולא “רשימת טפסים”.
- מצב כהה נשמר, אך עם פחות תיבות צבעוניות שמתחרות זו בזו.
- המותג והלוגו נשארים; הלוגו אינו מחליף היררכיית מידע.
- צבעים משרתים סוג מידע וסיכון, לא קישוט.

## צבעים סמנטיים

יש להשתמש בטוקנים ובשפה עקבית:

- רקע/משטח רגיל: כחול-לילה כהה
- הוצאות/סיכון תקציבי: חמר
- הכנסות/חיובי: מרווה
- תשלומים/דחיפות: גחלת
- מידע/אנשים/אירועים: כחול
- פעולה ראשית: שמנת/חול עם ניגודיות גבוהה

אין להסתמך על צבע בלבד. כל צבע משמעותי חייב לבוא עם טקסט, אייקון או label.

## טיפוגרפיה

- Heebo בלבד.
- טקסט גוף: לפחות 16px, עדיפות ל-17px.
- טקסט עזר: לא פחות מ-14px.
- מספרים: `font-variant-numeric: tabular-nums lining-nums`.
- אין `letter-spacing` בעברית.
- יש לשמור line-height נדיב וקריא.
- סכום חייב להישאר בשורה אחת לצד סימן מטבע ככל האפשר.

## מובייל

- Mobile-first אמיתי.
- כל אזורי המגע: לפחות 48px.
- אין גלילה אופקית ב-320px, 360px, 375px ו-390px, למעט אזורים שמוגדרים במפורש כטבלאות השוואה גלילות.
- תמיכה ב-`env(safe-area-inset-bottom)` ובמכשירי iPhone.
- הסרגל התחתון לא יסתיר תוכן או כפתורי שמירה.
- Bottom Sheet: גובה בטוח, כותרת דביקה, כפתור סגירה גדול, פעולה דביקה.
- שדות מספריים: `inputmode="decimal"` או `numeric` לפי הצורך.
- אין hover-only behavior.

## נגישות

- `aria-label` לכל כפתור אייקון.
- `aria-expanded` ו-`aria-controls` לאזורים מתקפלים.
- Focus visible ברור.
- תמיכה במקלדת.
- הודעות טעינה/שגיאה עם `role="status"` או `role="alert"` לפי הצורך.
- `prefers-reduced-motion` מבטל/מצמצם אנימציות.
- טקסטי פעולה ברורים בעברית.

---

# 5. הוראות הנדסיות

## שימור חוזי JavaScript

אין לשנות שמות או חתימות של פונקציות קיימות שמחוברות ל:

- `onclick` בקוד HTML שנוצר
- `google.script.run`
- ניווט קיים
- פתיחת Bottom Sheets
- שמירה, עריכה, ארכוב, שכפול וייבוא
- cache/lookups

מותר לבצע refactor פנימי רק אם נשמרת תאימות מלאה לכל הקריאות וההתנהגויות הקיימות.

## שימור נתונים

- אין לשנות IDs.
- אין לשלוח שדות חדשים לשרת.
- אין להסיר שדות קיימים ב-submit.
- אין לבצע normalisation חדש בלקוח שמשנה משמעות נתונים.
- אין לתקן סכומים אוטומטית לפי סטטוס.
- אין לשנות את ערכי הסטטוס.

## פעולות מסוכנות

- ארכוב תמיד דורש אישור UI ברור עם שם הרשומה.
- אין Hard Delete.
- אין שינוי סכימה.
- אין פריסה ללא אישור מפורש של המשתמשת.

---

# 6. Acceptance Criteria

היישום נחשב מוכן רק אם כל התנאים הבאים מתקיימים.

## פונקציונליות

- כל מסך קיים נטען ללא שגיאת JavaScript.
- כל פעולות היצירה, העריכה והארכוב עדיין נגישות ופועלות.
- אין פעולה קיימת שנעלמה.
- כל קריאות `google.script.run` ממשיכות להשתמש באותם שמות וארגומנטים.
- כל נתון קיים מוצג במקום כלשהו ובהקשר נכון.
- השכפול נשאר תקין.
- חיפוש, סינון, השוואה וייבוא נשארים תקינים.
- תשלומים, מקדמות ופיצולים נשארים תקינים.
- כיסוי נשאר תקין וללא Double Counting.
- חישובים פיננסיים נשארים זהים.
- לוח אמנים, שעות, משך, עלות לשעה וחפיפות נשארים תקינים.
- תזכורות והערות נשארות מקושרות לישות הנכונה.

## UX

- בתוך 10 שניות מכל מסך ברור מה הנושא, מה מצב העבודה ומה הפעולה העיקרית.
- מסך אירוע מסביר מצב הפקה לפני שהוא מעמיס פירוט כספי.
- כל המידע הכספי נשאר נגיש ומלא.
- קטגוריות ותתי-קטגוריות לא יוצרים מסך ארוך ולא סריק כברירת מחדל.
- טופס הוצאה מציג את כל השדות, אך בקבוצות משמעותיות.
- כל פעולה קריטית ניתנת לביצוע בנייד ביד אחת במידת האפשר.

## נגישות ורספונסיביות

- נבדק ב-320px, 360px, 375px ו-390px.
- אין גלילה אופקית כללית.
- אין תוכן מוסתר מאחורי ה-bottom nav.
- אין טקסט קריטי קטן מ-14px.
- כל פעולת אייקון כוללת label נגיש.
- כל אקורדיון נגיש במקלדת ומדווח מצב פתוח/סגור.
- צבע אינו האינדיקציה היחידה למצב.

---

# 7. תהליך הטמעה

## לפני שינוי

```bash
git status
git add -A
git commit -m "backup before full UI redesign"
```

אם יש שינויים לא מחויבים, לעצור ולבקש החלטה.

## קבצים שמותר לעדכן

```text
src/Index.html
src/Stylesheet.html
src/JavaScript.html
```

## קבצים שאסור לעדכן

```text
src/*.gs
appsscript.json
.clasp.json
Google Sheets data
```

## לפני פריסה

1. להפעיל את כל בדיקות השרת הקיימות בפרויקט.
2. להריץ בדיקת תחביר ל-JavaScript.
3. לפתוח את האפליקציה מול השרת האמיתי/תצוגה קיימת.
4. לבדוק את כל המסכים והפעולות לפי ה-Acceptance Criteria.
5. לבדוק מובייל ברוחבים 320/360/375/390.
6. להציג למשתמשת סיכום מלא וצילומי מסך/תצוגה.
7. לקבל אישור מפורש לפני כל `clasp push` או עדכון deployment.

## פריסה רק לאחר אישור

```bash
clasp push -f
clasp update-deployment AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ
```

---

# 8. עצירה מחייבת לקבלת אישור

יש לעצור ולא לבצע פעולה אוטומטית אם נדרש אחד מהבאים:

- שינוי לוגיקה עסקית או חישוב.
- שינוי API, שם פונקציה או ארגומנטים לשרת.
- שינוי סכמת Sheets או הוספת עמודה/טבלה.
- מחיקה או הסרה של יכולת קיימת.
- הוספת ישות חדשה, למשל Tasks או Commitments כטבלה חדשה.
- שינוי משמעות סטטוסים.
- שינוי תהליך שכפול/ארכוב/תשלום.
- פריסה לסביבת הייצור.

בכל מצב כזה יש להציג:

1. מה הבעיה או ההזדמנות.
2. למה היא לא נפתרת בשכבת UI בלבד.
3. חלופות אפשריות.
4. ההשפעה על נתונים קיימים.
5. בקשה לאישור מפורש.

---

# 9. הגדרת הצלחה

המוצר המוגמר אינו מוצר “פשוט יותר” עם פחות יכולות. הוא מוצר מלא שבו מפיק/ת אירועים יכול/ה:

- להבין מיד מה דחוף ומה מצב כל אירוע.
- לנהל את כל תחומי ההפקה דרך קטגוריות ותתי-קטגוריות.
- לראות אמנים, ספקים, ליינאפ, התחייבויות ותשלומים בהקשר של אירוע.
- להגיע לכל חישוב פיננסי מקצועי בלי לאבד הקשר תפעולי.
- ליצור, לערוך, לשכפל ולארכב במהירות ובבטחה בנייד.
- לעבוד עם כל המידע הקיים בלי עומס, בלבול או צורך לזכור איפה כל פעולה נמצאת.

**אין לצמצם את המערכת. יש להפוך אותה למערכת עשירה, מסודרת, מקצועית, נגישה ונוחה באמת.**
