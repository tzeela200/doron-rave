# DORON'S RAVE — מסמך מסירה (Handoff) · מעודכן 2026-09-14

מסמך זה מחליף את `doronsrave-package/HANDOFF.md` (שנשאר כהיסטוריה של שלב 1).
מיועד לכל מי שממשיך את הפרויקט, כולל Claude Code בסשן חדש: לא להתחיל מאפס ולא לסתור החלטות.

סדר קריאה:
1. המסמך הזה
2. `WORKLOG.md`: יומן שינויים מפורט עם אימות לכל מנה
3. **`CALCULATIONS.md`: כל חישוב במערכת, עם הנוסחה והמיקום בקוד.**
   חולץ מהקוד ונעול בבדיקות. לפני שנוגעים במספר — לקרוא שם.
4. `doronsrave-package/SPEC-מקורי.md`: האפיון הקנוני (100 סעיפים)

---

## 0. המערכת החדשה (`web/`) — מצב 2026-09-22

> ⚠ מכאן והלאה המסמך מתאר בעיקר את מערכת Apps Script הישנה (`src/*.gs`).
> המערכת החדשה נבנתה לפי חבילת הקנון 00–12 (2026-09-22), והיא גוברת על ההחלטות
> הישנות שסותרות אותה (למשל "בלי Supabase" בסעיף 2).

- **קוד:** `web/` — React + TS + Vite, Supabase `fvttcoavqmvzmqqgkmta` (לעולם לא פרויקט אחר).
- **פריסה:** GitHub `tzeela200/doron-rave` (branch `main`) → Vercel, Root Directory `web`.
  Push ל-`main` = פריסה ל-Production. לא לדחוף בלי אישור המשתמשת.
- **Production:** https://doron-rave.vercel.app
- **DB:** כל שינוי סכמה = קובץ חדש ב-`web/supabase/migrations/`, ומוחל על הפרויקט הנכון בלבד.

### 0א. עיצוב — מקור אמת (2026-09-22)
**קנוני:** `DORONS_RAVE_02_DESIGN_SYSTEM_CANON_V3_2026-09-22.docx` (מחליף את ערכי ספר 02 הקודם בכל
צבע/טוקן) + `DORONS_RAVE_UX_CHANGE_ADDENDUM_EVENT_HOURS_ACCORDIONS_2026-09-22.docx`.
**הוחלף:** מסמך "Design System Update v2" (אותו יום) — V3 סותר אותו במפורש (רדיוס, צבעים, תנועה)
והוא הגובר. **הנחיות עבודה (לא קנון):** פרומפט "Executive Luxury Refinement" — היררכיה, ריווח,
נוכחות KPI, כרטיס אירוע "חי". מיושמות רק היכן שאינן סותרות את V3.

נעול לפי V3:
- **פלטה בלבד:** רקע `#F7F7F4`, משטח `#FFFFFF`, טקסט `#1F2937`, טקסט חזק `#111827` (מספרים וכותרות),
  משני `#6B7280`, גבול `#E5E7EB`, כתום `#D97706` / חזק `#B45309` / רך `#FFF4E6`, טורקיז `#0F766E` / `#E7F6F4`,
  הצלחה `#16A34A` / `#ECFDF3`, אזהרה `#CA8A04` / `#FFF8E1`, שגיאה `#DC2626` / `#FEF2F2`. **אין כחול.**
- **מיפוי סמנטי (§5):** הכנסות טורקיז · נותר לשלם כתום · רווח ירוק / הפסד אדום · אירוע קרוב כתום ·
  ספירה/מידע טורקיז. הוצאות מוסכמות — ניטרלי (אין להן מיפוי).
- **KPI:** Icon Tile 40px (ריבוע מעוגל, רקע רך) → Label → Value (700, לא נשבר, מתכווץ בכרטיס צר) → שורה.
  בלי Delta.
- **גישה מהירה:** אירועים כתום · אמנים טורקיז · קטגוריות ירוק · ספקים ניטרלי. כל האריח ברקע Accent Soft,
  האייקון על Tile לבן. אריחים אחרים (ניהול) ניטרליים. **אין כחול — החלטת המשתמשת 2026-09-22.**
- **כרטיס אירוע:** רדיוס 16, Banner 72px מתמונת המותג (נכס קיים) + gradient כהה עדין, צל אחד.
- **רדיוס:** control 12 · card 16 · sheet 24. **תנועה:** 120/180/240, accordion 200. **צל** אחד.
- **כפתורים:** Primary `#B45309` · Secondary לבן · Tertiary שקוף + טורקיז · Danger Soft לארכיון.
- **מסך אירוע (Addendum §6):** Hero, תמונה פיננסית ומוכנות — גלויים. הוצאות, הכנסות, אמנים,
  ספקים, הערות, דוחות — Accordion סגור עם "כותרת (N)" + סיכום (הוצאות: סוכם/שולם/נותר;
  אמנים: טווח שעות; הערות: תזכורות פתוחות). כפתורי ההוספה נגישים גם כשסגור.
- **שעות אירוע:** `events.event_start_time` / `event_end_time` (TIME, nullable). Hero: `תאריך · 22:00–07:00 · 9 שעות`.
  שעה אחת — רק היא. משך רק כששתיהן קיימות; חוצה חצות נתמך.
  **התחלה == סיום = שגיאת ולידציה** (החלטת המשתמשת 2026-09-22): טופס, `save_event` (`event_hours_equal`)
  ו-check בטבלה (`events_hours_not_equal`). אין אירוע 24 שעות מרומז.
- **Hero אירוע:** אותו Banner כמו בכרטיס + תאריך / שעות / מקום כעובדות. **סימן המותג** בכותרת הבית — נשאר.
- **מעברים:** Accordion, Sheet ו-Dialog — 200ms.

- **Top Design Review (המשתמשת, 2026-09-22) — גובר על V3 בשתי נקודות, בהחלטה מפורשת:**
  (א) **שלוש שכבות צל** במקום צל יחיד: Flat (גבול) · Secondary `--shadow-1` (KPI, קיצורים, כותרות Accordion,
  כרטיסי תשלום) · Primary `--shadow-2` (כרטיס אירוע, Hero, סיכום). משטח מורם מוותר על הגבול.
  (ב) **כרטיס אירוע מוביל בבית** (האירוע הקרוב, רוחב מלא): Banner 136px עם שם האירוע ו"לאירוע" — חורג מ-V3 §8
  (48–72). שאר הכרטיסים נשארים 72px. סדר הבית לא השתנה (KPI למעלה).
- **קצב:** סקאלת כותרות רחבה (עמוד 26/28/32 · סקשן 20/22 · meta 13 · caption 12); ריווח משתנה בבית
  (אזור האירועים פותח באוויר, קיצורים צמודים יותר, הסיכום סוגר במרווח); שורות רשימה 16px.
- **רכיבים:** KPI — Tile 44, ערך עד 34/36 (מתכווץ בכרטיס צר), Label קטן, קו מפריד רך, hover. כרטיס אירוע =
  "כרטיס כניסה" (תלוש מחורר עם חריצים). תשלומים = Timeline (מסילה, נקודה, שבב תאריך, סכום גדול). קיצורי
  ניהול = Service tiles. ניווט: מחוון פעיל מונפש + גלולה מאחורי האייקון בנייד. אייקונים 22, Tiles 40/44.
- **מגע:** בנייד כל פקד ≥ 48px (compact = 48 מתחת ל-600px). **תנועה:** 200ms ease-out חלק; לחיצה = scale .985.
- **Overlays ב-portal:** Sheet/Dialog מוצגים ב-`<body>` — כך פעולה מ-Accordion סגור (למשל "הוסף הכנסה") פותחת Sheet גלוי.
- **מסך אירוע:** Summary → Financial → Accordions. מוכנות היא ה-Accordion הראשון, נפתח לבד כשהוגדרה רשימה.
- **מוכנות הפקה = צ׳ק־ליסט של עבודה (החלטת המשתמשת 2026-09-23):** לכל פריט ברשימה יש סטטוס ידני
  `completion_status`: לא התחיל / בטיפול / בוצע (`event_required_items`, ברירת מחדל "לא התחיל").
  **אחוז המוכנות = בוצעו ÷ סה״כ**, ולא לפי קיום הוצאה. הכיסוי הכספי (מכוסה / כלול / חסר) ממשיך
  להיגזר מההוצאות בדיוק כמו קודם, מוצג ליד כל פריט, וזמין כ-`coveragePercent` (שם נעשית ההשוואה
  לשיטה הישנה ב-reconcile). כסף, רווח, נקודת איזון ומבנה הנתונים הקיים — ללא שינוי.
  עריכת הרשימה שומרת את הסטטוס של פריטים שנשארו (`set_event_required_items` כבר לא מוחק הכול).
  **סטטוס אחד בכל שורה (2026-09-23, תיקון אחרי פידבק):** פריט **בלי** הוצאה מציג רק את הסטטוס הידני —
  חסר → בטיפול → בוצע — והתג עצמו הוא הכפתור. פריט **עם** הוצאה ממשיך להציג את הכיסוי (מכוסה / כלול),
  והעיגול בצד מסמן בוצע. אין יותר שני סטטוסים סותרים באותה שורה.
- **אחריות על פריט מוכנות (2026-09-23):** `event_required_items.owner_name` (טקסט חופשי, ברירת מחדל ריק,
  עד 60 תווים) + RPC `set_required_item_owner`. ב-UI: "דורון" / "צאלה" כבחירה מהירה, ושדה לשם אחר.
  השמות אינם קבועים בקוד כרשימה סגורה.
- **מסך אירוע (2026-09-23, בקשת המשתמשת):** כל הסקשנים סגורים כברירת מחדל, כולל מוכנות הפקה.
  לכל סקשן אייקון בצבע משלו מהפלטה: מוכנות ירוק · הוצאות כתום · הכנסות טורקיז · אמנים חרדל ·
  ספקים ירוק · הערות כתום · דוחות טורקיז. ה-KPI "הכנסות" הוא כפתור שפותח את סקשן ההכנסות וגולל אליו
  (`openSection(id)` ב-`layout.tsx`).
- **ליין־אפ:** כל שורה מציגה שעות + משך, שם האמן, "שם במה" ו"שם פרטי" כשהם שונים מהשם המוצג, שם
  ההוצאה כשהוא שונה, ואת הסכום המוסכם + עלות לשעה. שום שם לא מומצא ולא חוזר פעמיים.
- **מוכנות הפקה מוצגת בספירה, לא באחוזים** (המשתמשת, 2026-09-23).
- **תמונת אירוע (2026-09-23, בקשת המשתמשת):** `events.image_path` + RPC `set_event_image`
  (מאמת שהנתיב שייך לאירוע). הקובץ נשמר ב-bucket הפרטי הקיים תחת `event/{event_id}/…`, והתצוגה דרך
  Signed URL לשעה (`useEventImageUrl`). כפתור "העלה תמונה" נמצא **בעריכת אירוע** ונשמר מיד בבחירת הקובץ,
  בנפרד משמירת הטופס. JPG/PNG/WEBP עד 8MB. אין תמונה → הבאנר חוזר לתמונת המותג המשותפת.
  הסרה מנקה את הקישור בלבד; הקובץ נשאר ב-Storage (Book 03 §15).
- **סכומים בהוצאה (2026-09-23):** הקלדה בשדה אחד ממלאת את השני, עד ששדה נערך ידנית או שכבר היה בו ערך.
  שני הסכומים נשארים שדות נפרדים; אין שינוי בחישובים.
- **פריסה:** `web/vercel.json` — כל נתיב שאינו קובץ חוזר ל-`index.html` (רענון במסך פנימי לא מחזיר 404);
  `sw.js` בלי cache.

## 1. סטטוס

> 2026-09-14: המשתמשת דיווחה שהריצה את `seedCategoryTaxonomy()` וייבאה את 10 אמני ה-DJ לנתונים האמיתיים.

### גישה
- **אפליקציה:** https://tinyurl.com/doronsrave
  (מפנה ל-Web App: `.../macros/s/AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ/exec`)
- **פריסה:** Deployment ID `AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ`, גרסה נוכחית: @23 (נפרסה 2026-09-20).
- **Script ID:** נמצא ב-`.clasp.json`, עם `rootDir: src`.
- **Spreadsheet ID:** נמצא ב-Script Properties בלבד, לא בקוד. אלה שני מזהים שונים.

### קיים ועובד (נבדק)
- **נתונים ובסיס:**
  - Data Layer גנרי עם `LockService`.
  - `initializeSystem()` אידמפוטנטית.
  - פורמט עמודות (טקסט/תאריך/מספר).
  - תאריכים מוחזרים ל-Frontend כמחרוזת, כי `google.script.run` לא מעביר אובייקט Date.
- **ישויות:**
  - Events, Categories + **Subcategories**, Vendors, Artists, Expenses.
  - Payments / מקדמות, כולל "באיחור" מחושב, ו-Income.
  - אמצעי תשלום (הוספה/השבתה).
- **יכולות:**
  - תשלומים קרובים.
  - שכפול אירוע ("הכל כבסיס" / "מבנה בלבד").
  - השוואת אירועים.
  - חיפוש וסינון.
  - ייבוא רשימות (אמנים/ספקים).
  - Breakdown לפי קטגוריה (עם פירוט תת-קטגוריה) ולפי ספק.
- **ממשק:**
  - עיצוב v4 נגיש: גופן Heebo, טקסט 17px (מינימום 14px), כחול-לילה, פאנלים צבעוניים, אריחים 2 בשורה.
  - RTL, Mobile First.
- **שני משתמשים** (ראו החלטה 3).

### שדרוג 2026-09-16 (נפרס @12)
- **סכימה:** `Notes` (Tab חדש); `Events.average_ticket_price/expected_ticket_count`; `Artists.real_name/stage_name`;
  `Expenses.performance_start_time/performance_end_time`; `Income.is_ticket_income`. הכול **בסוף** הטבלאות.
- **`SchemaService.gs`:** `ensureEnhancementSchema_()` / `upgradeSchema()` — מוסיף רק מה שחסר, עוצר בשגיאה אם סדר העמודות לא תואם.
- **P&L:** `computeEventPnl_()` — הוצאות מתוכננות (עם נפילה לסכום שסוכם), כרטיסים שנמכרו, נקודת איזון ותחזית רווח.
- **אמנים:** שם במה + שם אמיתי, שעות הופעה, משך, עלות לשעה, לוח אמנים כרונולוגי ואזהרת חפיפה.
- **`NotesService.gs`:** הערות ותזכורות מתוארכות לאירוע ולאמן.

### שדרוג מוצרי 2026-09-16b (נפרס @13)
מקור: `אפיון שדרוג מלא למערכת הקיימת.docx` — P0 + P1 בלבד.
- **`ExpenseCoverage` (Tab חדש):** "מה כלול במחיר". **לא נוספה אף עמודה לטבלה קיימת.**
- **`CoverageService.gs`:** סנכרון הרכיבים ב-Lock אחד; ביטול סימון = ארכוב; סימון חוזר מחיה שורה במקום לשכפל.
- **`AttentionService.gs`:** "דורש טיפול" — תשלומים באיחור/השבוע ותזכורות פתוחות, מנתונים קיימים בלבד.
- **תיקון באג:** `getInitialData` הציג את האירוע **המאוחר** ביותר. עכשיו הקרוב ביותר בעתיד. גם `getEvents` ממוין כך.
- **מסך הבית:** דשבורד — פוסטר, פעולות מהירות, דורש טיפול, אירועים קרובים, מצב כספי, תשלומים, ניהול, היסטוריה.
- **מתוכנן / מוסכם / שולם / נותר** כארבעה מושגים נפרדים בבית ובמסך האירוע.
- **מסך אירוע:** סיכום ← דורש טיפול ← פעולות מהירות ← "מה סגור" ← הוצאות ← הכנסות ← P&L ← לוח אמנים ← חלוקה ← הערות.
- **שדרוג הסכימה:** רץ אוטומטית בכתיבה הראשונה אחרי הפריסה ויוצר את `ExpenseCoverage`.
  אפשר גם `upgradeSchema()` ידנית מהעורך. אידמפוטנטי.
- **שדרוג הסכימה:** רץ אוטומטית בפעולת הכתיבה הראשונה אחרי הפריסה, ואפשר גם להריץ ידנית `upgradeSchema()` מהעורך. אידמפוטנטי.

### עוד לא נבנה
- Attachments (קבלות ל-Drive).
- ליטוש נגישות סופי (WCAG AA).
- **ניקוי נתוני בדיקה לפני עבודה אמיתית (סעיף 96):** בלתי הפיך מבחינת המשתמשת, ולכן רק באישור מפורש שלה.

### 2יב. מצב ההפקה — הרשימה היא של המשתמשת (2026-09-20, נפרס @22)
99. **`EVENT_REQUIREMENTS` בוטלה.** 11 השלבים הקבועים ש-Claude המציא הוסרו מהשימוש
    (השם שונה ל-`EVENT_REQUIREMENTS_LEGACY_` ונשאר לתיעוד בלבד). **אין להחזיר אותה.**
100. **`Events.required_items`** — עמודה חדשה **בסוף** הטבלה, מחזיקה את מה שהמשתמשת
    סימנה שהאירוע צריך, כמפתחות `sub:<id>` / `cat:<id>` מופרדים בפסיק.
101. **ההיגיון הפוך מקודם:** כלום לא מסומן מראש, והמשתמשת בוחרת מה האירוע צריך
    מתוך הקטגוריות שלה. קודם 11 היו מסומנים והיא מחקה.
102. **אירוע שלא הוגדר לא מציג אחוז מוכנות — גם לא 0%.** אין מכנה, אין מספר.
    `summarizeEventReadiness_` מחזיר `configured: false`, והממשק מזמין להגדיר.
103. **ההתאמה לפי מזהה ולא לפי שם.** `buildOperationalCoverage_` כבר מייצר מפתחות
    `sub:`/`cat:`. שינוי שם של תת-קטגוריה לא שובר את הרשימה — נעול בבדיקה.
104. **`setEventRequirement(eventId, key, needed)` שמר על החתימה** וקיבל משמעות חדשה:
    מוסיף או מסיר רכיב. נוסף `setEventRequiredItems(eventId, keys)` לשמירה בבת אחת.
105. **בחירת רכיבים לא משנה שום סכום.** נעול בבדיקה.

### 2יד. סבבי תמחור לכרטיסים (2026-09-20, נפרס @23)
109. **`Events.ticket_tiers`** — עמודה חדשה **בסוף** הטבלה, JSON של
     `[{name, quantity, price}]` **לפי סדר המכירה**.
110. **נקודת האיזון נמכרת לפי הסדר** (`ticketsToCover_`): ממלאים את הסבב הראשון,
     וכשהוא נגמר עוברים לבא. **לא ממוצע משוקלל.**
111. **כשכל הסבבים יחד לא מכסים את העלות** — `break_even_tickets = null` ו-
     `break_even_unreachable = true`. **לעולם לא מוצג כ-0.**
112. **סבבים גוברים על `average_ticket_price` ו-`expected_ticket_count`.**
     בלי סבבים — החישוב נשאר בדיוק כפי שהיה. נעול ב-`test-tiers.js`.
113. **`average_ticket_price` גובר על הממוצע בפועל** (החלטה 24, נשארת).
     זו מלכודת למי שמוכר בכמה מחירים — מתועדת ב-`CALCULATIONS.md`.

### 2טו. שמות: "למערכת" מול "לאירוע" (2026-09-20)
114. **"הוספת אמן/ספק למערכת"** = רשומה בלבד. שם, טלפון, הערות. בלי כסף ובלי אירוע.
     **"הוספת אמן לאירוע"** = הוצאה. שעות, עלות, שיוך.
     הכפתור והכותרת חייבים להגיד את אותו דבר — כפתור "הוסף אמן לאירוע" שנפתח
     כ"הוספת הוצאה" הוא מה שבלבל את המשתמשת.
115. **`openExpenseSheet(eventId, expenseId, intent)`** — `intent: 'artist'` משנה את
     הכותרת ומסמן מראש את קטגוריית האמנים. אותו טופס, אותה שמירה.

### 2טז. החלוקה לפי ספק מסתכמת (2026-09-20)
116. **הוצאה בלי ספק נאספת ל"ללא ספק"** (`NO_VENDOR_LABEL`) במקום להישמט,
     כדי ששקלים לא ייעלמו מהחלוקה. **זה לא חל על בלוק "ספקים ואנשי קשר"** —
     שם אמן באמת לא אמור להופיע (החלטה 106). חלוקה היא כסף; הבלוק הוא אנשים.

### 2יג. אמן אינו ספק (2026-09-20)
106. **"ספקים ואנשי קשר" מציג ספקים בלבד.** קודם הוא נבנה מ-`artist_name || vendor_name`
     ולכן אותו אמן הופיע בשני בלוקים.
107. **"אמנים וליין אפ" מציג את כל האמנים**, לא רק את המשובצים: המשובצים לפי רצף
     הלילה, ואחריהם "אמנים בלי שעות". בלי זה אמן בלי שעות היה נעלם משני הבלוקים.
108. **שעות הופעה זהות נחסמות בשמירה.** `22:00–22:00` נתן 0 דקות, והכלל
     "סיום ≤ התחלה = חציית חצות" הפך את זה ל-24 שעות ועיוות את "עלות לשעה".
     חציית חצות חלה מעכשיו על הפרש **שלילי** בלבד.

### ⏸ פתוח להחלטה — היסטוריה (הוכרע 2026-09-20)
**זו לא משימת פיתוח, זו החלטת מוצר שממתינה למשתמשת. אין לגעת בלי הכרעה שלה.**

- **הבעיה:** `EVENT_REQUIREMENTS` — 11 שלבי הפקה — **הומצאו על ידי Claude** בסשן של 2026-09-16,
  מסיבה הנדסית (להימנע מטבלה חדשה). המשתמשת מעולם לא בחרה אותם ואינה יכולה להוסיף להם
  מהממשק. **ו"36% מוכן" שמופיע על כל כרטיס אירוע מחושב מהמכנה הזה.**
- **מה היא אמרה:** *"מי קבע את ה-11 שלבים האלו? מצב ההפקה לא תלויה ב-11 פרמטרים...
  אמור להיות כל אותם דברים שעשיתי או לא עשיתי. אולי יהיה נכון לשים את הקטגוריות
  ואז אני אסמן אם צריך או לא צריך."*
- **הממצא שתומך בה:** כל 11 השלבים כבר יושבים בטקסונומיה שלה (10 תתי-קטגוריות + הקטגוריה
  "אמנים"), אבל **25 תתי-קטגוריות נשארות בחוץ** — ובהן גדרות · אוהלים · שילוט · הסעות ·
  פחי אשפה · ציליה · צלם · פרסום · צוות הקמה/פירוק. אירוע בלי הסעות יראה "36% מוכן"
  ולא ירמוז במילה.
- **הכיוון שהיא הציעה:** הרשימה = הקטגוריות שלה, והיא מסמנת מה האירוע צריך.
  ההיגיון מתהפך: היום 11 מסומנים כ"נדרש" והיא מוחקת; במוצע כלום לא מסומן והיא בוחרת.
- **המחיר:** עמודה חדשה **בסוף** `Events` למה שסומן "צריך". שינוי סכימה — דורש אישור מפורש.
- **סטטוס:** המשתמשת אמרה "מתייעצת". ממתין.

### ממתין להחלטה (2026-09-18)
- **`getEvents` לא מחזיר `readiness` ו-`pnl`.** לכן אותה שורת אירוע עשירה יותר בבית
  (36% מוכן · 6 חסרים · כרטיסים לאיזון) מאשר במסך "אירועים". הסגירה היא קריאה ל-
  `summarizeEventReadiness_` בתוך `getEvents` — פונקציה שכבר קיימת ומשמשת את `getInitialData`.
  **לא בוצע** כי החלטה 62 נועלת את השינוי לשלושת קבצי הממשק. דורש אישור לנגיעה ב-`.gs`.

### ⛔ עיצוב בהיר (Light UI) — נבנה ונדחה, אין לבנות מחדש
`DORONS_RAVE_LIGHT_UI_PRODUCER_EXPERIENCE_CLAUDE_BRIEF_2026-09-20.md` קיים בשורש הפרויקט.
**מנות 1–2 שלו נבנו במלואן ב-2026-09-20, והמשתמשת ביקשה לבטל: "לא לא. בטל את השינויים.
לא מעוניינת."** הביטול בוצע, והקבצים הוחזרו byte-for-byte.
**אין להתחיל לבנות מהבריף הזה בלי בקשה מפורשת ומחודשת.**
מה שכן נלקח ממנו, לבקשתה: הורדת "דורש טיפול" בלבד (החלטות 88–93).

### V5 (2026-09-18) — שפת בלוקים אחת (נפרס @20 ב-2026-09-19)
שכבת UI בלבד: `Stylesheet.html` + `JavaScript.html`. `Index.html` ו-`.gs` לא נגעו.
בלוק גיבור צבעוני עם טבעת מוכנות · ארבעה אריחי חישוב בראש כל אירוע שפותחים את הבלוק שמאחוריהם ·
פעולות מהירות כאריחים · אותו `.stat-tile` בבית ובאירוע · שורות אירוע, אנשים ותשלומים כבלוקים צבעוניים.
ההחלטות ב-2ט. נקודת שחזור: `.ui-backup/v19-before-v5/`.


---

## 2. החלטות נעולות — לא לסטות בלי לשאול את המשתמשת

1. **Google Apps Script + Google Sheets בלבד.**
   - בלי Supabase/Firebase/DB חיצוני, בלי API נפרד.
   - תקשורת רק דרך `google.script.run`.
2. **Vanilla JS, בלי build step.**
   - Index.html + Stylesheet.html + JavaScript.html דרך `include()`.
   - Tailwind לא הוכנס. עקרונות hebrew-tailwind-preset יושמו ב-CSS: סולם עברי, logical properties.
3. **שני משתמשים:**
   - `executeAs: USER_ACCESSING`, `access: ANYONE`.
   - **הרשאה בפועל = שיתוף קובץ ה-Sheets כעורך.** אין רשימת משתמשים בקוד.
   - כניסה ראשונה: מסך "Google לא אימתה" ← מתקדם ← המשך. זה צפוי.
4. **Spreadsheet ID רק ב-Script Properties.**
5. **מחיקה = Archive תמיד** (`is_archived`), אף פעם לא Hard Delete. אמצעי תשלום: השבתה (`is_active`).
6. **אמן הוא סוג הוצאה**, לא ישות כספית מקבילה. אין Double Counting.
7. **שכפול לא משנה את המקור.**
   - IDs חדשים לכל Event/Expense/Payment.
   - תשלום משוכפל לעולם לא "שולם".
   - מועדי תשלום זזים לפי הפרש תאריכי האירועים.
8. **עברית מלאה, RTL**, בלי מונחים טכניים ב-UI.
9. **עיצוב (v4, 2026-09-15, לפי israeli-ui-design-system):**
   - טוקנים בראש `Stylesheet.html`. לא ממציאים צבעים.
   - הוצאות = חמר, הכנסות = מרווה, תשלומים = גחלת, אירועים = כחול.
   - **גופן: Heebo בלבד** (בחירת המשתמשת, 2026-09-15).
     - Karantina הוסר: המשתמשת התלוננה שהכתב קטן, שהגופן לא לטעמה, ושבו 0 ו-8 נראים זהים.
     - Rubik נוסה לפני Heebo, והמשתמשת העדיפה את Heebo.
     - ספרות ברוחב אחיד (`tabular-nums`).
   - **טקסט:** גוף 17px, מינימום 14px, line-height 1.7, **אסור letter-spacing בעברית**, word-spacing 0.05em.
   - **מרווחים ונגישות:** מרווחים נדיבים, אזורי מגע ≥ 48px, טקסט משני בניגודיות WCAG AA.
   - **היררכיה קטגוריה ← תת-קטגוריה** (2026-09-15; המשתמשת ביקשה שיהיה הבדל ברור). אותה שפה בכל מקום:
     - **הורה:** כרטיס, אייקון בתיבה, שם מודגש, תג ספירה, חץ מסתובב.
     - **ילד:** אזור שקוע עם רקע כהה, קו-ענף, טקסט רגיל בצבע משני, כפתור הוספה מקווקו.
     - **טופס הוצאה:** השדה "תת-קטגוריה של X" מוזח עם קו.
     - **שורת הוצאה:** "קטגוריה ← תת-קטגוריה".
     - **חלוקת הוצאות:** פירוט באזור שקוע.
     - **אמנים:** כרטיס נפרד בגוון חמר עם אייקון מיקרופון.
   - **נוחות (2026-09-15):**
     - כפתור "חזרה" בכותרת, עם היסטוריית ניווט (`navStack`). כפתורי הסרגל התחתון מאפסים אותה (`root: true`).
     - אחרי יצירה או שכפול של אירוע, "חזרה" מדלגת על הטופס (`replace: true`).
     - כפתור שמירה דביק בתחתית כל Bottom Sheet.
     - ₪ בתוך שדות סכום (`.money-input`).
     - ספקים/אמנים ואירועים ככרטיסים.
     - בלי טקסטורת גרעיניות. אנימציית כניסה קצרה (0.28s).
10. **"מועד תשלום" בטופס ההוצאה:**
    - מנהל **תשלום יחיד**, רק במצב פשוט (0 תשלומים, או תשלום אחד בגובה ההוצאה).
    - בפיצול, הניהול עובר ל"פרטים נוספים ← תשלומים ומקדמות".
11. **מניעת Double Submit:** כפתור מושבת ב-UI + `LockService` בשרת.

### 2א. קטגוריות ותתי-קטגוריות (החלטות 2026-09-14)
12. **טבלת מילון `Subcategories`:**
    - עמודות: `subcategory_id` (UUID), `category_id` (הורה), `name`, `sort_order`, `is_visible`, `is_archived`, `created_at`, `updated_at`.
    - השם ייחודי **בתוך אותה קטגוריה** בלבד. "שונות" קיימת תחת 4 הורים.
13. **`Expenses.subcategory_id`:**
    - נוסף **בסוף** הטבלה.
    - אופציונלי: ריק = "ללא תת-קטגוריה".
    - השרת מאמת שתת-הקטגוריה שייכת לקטגוריה שנבחרה.
14. **"אמנים":**
    - **ללא תתי-קטגוריות, ולא נוגעים בלוגיקה שלה** (בחירה מתוך Artists, `isCategoryArtists` לפי שם).
    - השרת מאפס `subcategory_id` להוצאות אמנים, וחוסם יצירת תת-קטגוריה תחתיה.
15. **שדה תת-קטגוריה מופיע לכל קטגוריה שאינה "אמנים"**, כולל קטגוריות עתידיות.
    - אפשר ליצור תת-קטגוריה תוך כדי מילוי הטופס.
    - אין עמודה חדשה ב-Categories.
16. **Seed:**
    - `seedCategoryTaxonomy()` חד-פעמית ואידמפוטנטית, מורצת ידנית מהעורך.
    - הנתונים ב-`DEFAULT_SUBCATEGORY_TAXONOMY` (`Config.gs`): 6 קטגוריות, 36 תתי-קטגוריות.
    - **לא** חלק מ-`initializeSystem`, כי התקנה נקייה = "אמנים" בלבד (סעיף 11).
17. **אין מיגרציה:** הנתונים הקיימים הם נתוני בדיקה בלבד (לפי המשתמשת). קטגוריות ישנות לא מומרות.
18. **תאימות לפני הרצת initializeSystem:**
    - `ensureSubcategorySchema_()` יוצרת את הטבלה והעמודה בשימוש הראשון, אידמפוטנטית ועם Lock.
    - לכן העדכון לא שובר את האפליקציה בזמן הפריסה.
19. **ארכוב תת-קטגוריה:**
    - נעלמת מהבחירה.
    - הוצאות היסטוריות ממשיכות להציג את שמה (מפת השמות כוללת ארכיון).
### 2ב. שדרוג 2026-09-16 — החלטות נעולות
21. **סטטוסים לא השתנו.** אין סטטוס "מקדמה" — מקדמה היא תשלום חלקי.
22. **`computeEventTotals_` לא משנה משמעות.** כל התמונה המורחבת נמצאת ב-`computeEventPnl_` בלבד.
23. **הוצאות מתוכננות:** `planned_amount > 0 ? planned_amount : agreed_amount`.
24. **מחיר הכרטיס לחישוב איזון:** קודם `Events.average_ticket_price`, אחריו הממוצע בפועל, אחרת אין נקודת איזון.
25. **`Artists.name` נשאר לתאימות ולא עובר migration.** שם תצוגה: `stage_name || name || real_name`. ייבוא רשימה כותב `name` בלבד.
26. **שעות הופעה** שייכות להוצאה (אמן × אירוע), בפורמט `HH:mm`, ורק בקטגוריית אמנים. מעבר לקטגוריה אחרת מנקה אותן באותה הוצאה.
27. **חצות:** End קטן מ-Start = חציית חצות. מיון הלוח לפי "רצף לילה" (`nightSortKey_`), לא לפי מחרוזת.
28. **חפיפה = אזהרה בלבד**, לא חוסמת שמירה. נגיעה בקצה (22:00–23:00 מול 23:00–01:00) אינה חפיפה.
29. **Notes:** `entity_type` ∈ event/artist, `note_type` ∈ note/reminder. מחיקה = Archive. שכפול אירוע לא מעתיק הערות.
30. **`Events.general_notes` נשאר כפי שהוא** ולא מומר להערות מתוארכות.
31. **שכפול:** "הכל כבסיס" מעתיק שעות הופעה; "מבנה בלבד" שומר את האמן בלי השעות.

### 2ג. שדרוג מוצרי 2026-09-16b — החלטות נעולות
32. **Coverage הוא מידע תפעולי בלבד.** לעולם לא נספר כספית, לא משנה סכום, ולא מחלק את מחיר העסקה בין הרכיבים.
    הוצאה נספרת פעם אחת בלבד לפי ה-`category_id`/`subcategory_id` שלה.
33. **"מה כלול" מופיע רק בתת-הקטגוריה "השכרת שטח"** (`VENUE_SUBCATEGORY_NAMES` ב-Config, זיהוי לפי שם כמו "אמנים").
34. **אין דגל "כולל" בטבלת Expenses.** הוצאה היא עסקה כוללת אם יש לה שורת Coverage פעילה. "כן" בלי רכיבים לא נשמר — זה בסדר, כי זו רק אינדיקציה.
35. **ביטול סימון רכיב = `is_archived`.** לעולם לא מחיקה. סימון חוזר מחיה את אותה שורה.
36. **יציאה מ"השכרת שטח" מארכבת את הכיסוי** של אותה הוצאה. ארכוב הוצאה משחרר את הרכיבים שלה.
37. **כפילות כיסוי = אזהרה רכה בלבד.** אף פעם לא חוסמת שמירה.
38. **שכפול:** "הכל כבסיס" מעתיק Coverage; "מבנה בלבד" לא — עקבי עם החלטה 31.
39. **מסך הבית הפך לדשבורד באישור מפורש של המשתמשת (2026-09-16).**
    - **הפוסטר נשאר** — הוא חלק מהשפה העיצובית. רק הוקטן בגובה, ושורת המספרים המשניים עברה לפאנל "מצב כספי".
    - שפת העיצוב לא השתנתה: אותם טוקנים, Heebo, RTL, פאנלים צבעוניים.
40. **הניווט התחתון לא השתנה:** בית / אירועים / + / השוואה / הגדרות.
41. **כפתורים במסך צר גולשים או נגללים** (`.quick-actions`), ולעולם לא מתכווצים מתחת ל-48px גובה או 14px טקסט.
    נבדק ב-390px (שתי שורות) וב-320px (גלילה אופקית).
42. **"מה סגור באירוע":** ✓ מכוסה / ✓ כלול ב-X / • בתהליך. **אין "חסר"** — אין עדיין רשימת "צרכים לאירוע" שממנה לגזור חוסר.
    "מכוסה" נשען על `computed_status` הקיים — לא נוצר סטטוס חדש.
43. **`computeEventTotals_` עדיין לא השתנתה.** "מתוכנן" במסך האירוע מגיע מ-`computeEventPnl_.planned_expenses`.

### 2ד. עקרונות מוצר נעולים (2026-09-16, פידבק יועץ)
44. **Home הוא Dashboard, לא עמוד של אירוע.** השאלה הראשונה שהמסך עונה עליה: "מה לעשות עכשיו".
45. **כל שורה במרכז המשימות מנוסחת כפעולה**, לא כדיווח, ועם שם האדם כשיש כזה.
    עד שלושה בשמם; מעבר לזה — שורה מקובצת אחת.
46. **הפוסטר נשאר בבית** (הכרעת המשתמשת, שלוש פעמים) — אבל **מתחת** למשימות ול-KPI.
47. **ארבעה KPI, לא ששה.** "אירוע הבא" הוא הפוסטר; "הפעולה הבאה" היא מרכז המשימות.
48. ~~אין שפת צבעים חדשה.~~ **בוטל ב-2026-09-17 לפי V2:** Accent סמנטי בחמישה גוונים
    (כספים ירוק · אמנים סגול · ספקים כחול · טיפול כתום · דחוף אדום), **רק** על אייקונים, כותרות,
    Badges ופס עליון דק. בלי רקעים צבעוניים ובלי שינוי Theme. `--violet` הוא הטוקן היחיד החדש.
49. **אין ישויות חדשות בלי שהצורך התבסס.** רשימת הצרכים תהיה קבועה ב-`Config.gs` ועמודה אחת
    בסוף `Events` שמחזיקה רק את מה שסומן "לא צריך". אין Tab `EventNeeds`.
50. **ברכה בלי שם** — שני משתמשים, וברכה בשם הלא נכון גרועה מברכה בלי שם.
51. **השרת שומר בדיוק מה שנשלח אליו.** אין תיקון סכומים אוטומטי לפי סטטוס: ברירת המחדל של
    `normalizeManualStatus_` היא "מתוכנן", ולכן תיקון כזה היה מאבד `agreed_amount` לכל קורא
    שלא שולח סטטוס (ייבוא, שכפול, API). התיקון שייך לטופס. נעול ב-`test-stage0.js`.

### 2ה. מסך האירוע (2026-09-16, שלב 2)
52. **מסך האירוע מתחיל באירוע, לא בכסף.** המספר הגדול היחיד הוא "כמה ימים לאירוע".
    היתרה והרביעייה הכספית נמצאות ב-Accordion "תמונה פיננסית", בתחתית.
53. **`EVENT_REQUIREMENTS` קבוע ב-`Config.gs`** — 11 שלבי הפקה. אין ישות חדשה ואין Tab חדש.
54. **רק חריגים נשמרים.** `Events.skipped_requirements` (עמודה בסוף) מחזיקה את מה שסומן "לא צריך".
    אירוע בלי אף סימון = כל הרשימה נדרשת.
55. **ארבעה מצבים נגזרים, אף אחד לא מוקלד:** מכוסה / כלול ב-X / בתהליך / חסר.
    "לא צריך" הוא היחיד שהמשתמשת מזינה, והוא **מוציא את הרכיב מהחוסרים ומהמונה**.
56. **הוצאת אמן סוגרת את הצורך "אמנים".** `buildOperationalCoverage_` כולל את קטגוריית האמנים.
57. **רכיב נספר פעם אחת.** מה שסגר צורך ברשימה הקבועה (`matched_name`) לא חוזר ב"סגור גם".
58. **Accordion: אחד פתוח בכל פעם**, והסיכום בכותרת גלוי גם כשסגור.
59. **אחוז המוכנות מותר רק כי הוא אמיתי.** הוא נגזר מ-`EVENT_REQUIREMENTS` ומהכיסוי בפועל,
    ולא מציון מומצא. מוצג על הפוסטר ועל כל שורת אירוע עתידי, ולעולם לא על אירוע שעבר.
60. **הבית מציג את כל האירועים העתידיים**, לא רק את הקרוב.
61. **אזור הניהול הוא תפריט ולכן רשימה מקופלת** — לא גריד שמתחרה על תשומת הלב עם מידע ניהולי.

### 2ו. עיצוב מחדש של שכבת הממשק (2026-09-17)
62. **גבול השינוי:** `Index.html`, `Stylesheet.html`, `JavaScript.html` בלבד.
    אין לגעת ב-`.gs`, בסכימה, בשמות פונקציות שרת, בחישובים או בנתונים.
63. **`check-ui.js` הוא רשת הביטחון.** הוא נועל 40 endpoints ו-81 פעולות משתמש מול `.ui-backup/`.
    יש להריץ אותו אחרי כל שינוי ממשק. פעולה שנעלמת = כשל.
64. **תיק האירוע בנוי מחמש קבוצות עבודה:** מצב האירוע · הפקה · אנשים · כספים · תיעוד.
    הכסף בסוף — זה מסך עבודה, לא דוח.
65. **הקטגוריות הן לוח ההפקה.** בתוך אירוע הן מציגות מצב וסכומים; בהגדרות הן מילון לעריכה.
    אותה תשתית נתונים, שתי חוויות.
66. **אין מסך שפותח את כל התוכן שלו.** אקורדיון: הכול סגור, אחד נפתח, הסיכום בכותרת גלוי תמיד.
67. **`[hidden]` חייב `!important`.** בלעדיו כלל class עם `display` משאיר אקורדיון "סגור" פתוח.
68. **`getEvents` לא מחזיר `is_upcoming`** — נגזר בצד הלקוח ב-`isUpcomingEvent()`.
69. **מסך האדם משתמש ב-`getArtistExpenseHistory`** שקיים בשרת מאז שלב 1. אין endpoint חדש.
70. **רצפת נגישות:** אזור מגע **≥48px** (אפשר ב-`::after` שקוף כשהגובה החזותי חייב להישאר קטן),
    טקסט ≥14px כולל סמל המטבע, `aria-label` לכל כפתור אייקון, `aria-expanded` לכל אקורדיון,
    `role="status"` בטעינה ו-`role="alert"` בשגיאה, `:focus-visible`, `prefers-reduced-motion`,
    ובלי צבע כאינדיקציה יחידה.

### 2ז. מרכז הפעולות (2026-09-17, V2)
71. **שני סוגי פריטים, ואסור לערבב אותם.**
    **התראת מערכת** נוצרת לבד ונעלמת לבד — אין לה "סמן כטופל".
    **משימה אישית** (Notes) היא של המשתמשת — השלמה, דחייה, עריכה ומחיקה.
72. **אין להמציא התראות.** כל התראה חייבת להצביע על שדה קיים. אסור להסיק ש"משהו לא אושר"
    או "חסר" בלי שדה מפורש. המקורות המאושרים מתועדים ב-`actionCenterHtml`.
73. **חפיפת אמנים מוצגת רק במסך האירוע** — `getInitialData` לא נושא את הליינאפ,
    והצגתה בבית תדרוש שינוי שרת שלא אושר.
74. **ה-FAB אחראי על כל היצירה** (שש פעולות). הניווט נמצא באריחים. אין כפילות.

### 2ח. אריחי הניהול (2026-09-17, בקשת המשתמשת)
76. **אזור הניהול הוא גריד של אריחים צבעוניים** — לא רשימה מקופלת ולא בלוק נפרד של "גישה מהירה".
    זו היתה בקשה מפורשת אחרי שהמשתמשת הראתה צילום של הגרסה הישנה — והיא מבטלת את החלטה 61.
77. **כל אריח צבוע, והכתב לבן (#fff).** מונה גדול מעל התווית, ואייקון בגוון האריח.
    הגוונים נשלטים ב-`--tile-a/-b/-line/-accent`, ולכן גוון חדש הוא שורה אחת.
75. **KPI שלילי מוצג כ"הפסד צפוי"** עם ערך חיובי, לא כ"רווח צפוי ‎-26,000".

### 2ט. שפת בלוקים אחת — V5 (2026-09-18, בקשת המשתמשת עם צילומי מסך)
78. **בלוק אחד, בכל המערכת:** פס כותרת צבוע (אייקון בתיבה + כותרת לבנה + ערך לבן)
    מעל גוף כהה. הצבע חי בכותרת; הגוף נשאר `--ink-raised` כדי שהתוכן הלבן ייקרא.
    **אין יותר "פאנל חיוור"** — הגוונים הישנים (`--tint-*`) כבר לא צובעים גוף של פאנל.
79. **הגוונים הם מחלקות עצמאיות.** `.t-clay/.t-sage/.t-sky/.t-sand/.t-ember/.t-violet`
    מגדירות `--tile-a/-b/-line/-accent`, ואריח, בלוק, שורת אירוע וכרטיס אדם שואבים מהן.
    **גוון חדש = שורה אחת.** אין להמציא צבע מחוץ לשש המחלקות האלה.
80. **מספר ראשי הוא תמיד `.stat-tile`.** אותו רכיב בדיוק משרת את ה-KPI בבית ואת
    ארבעת החישובים בראש מסך האירוע. אסור לבנות מחדש אריח מספר לכל מסך.
81. **כל אריח חישוב נושא שני מספרים:** הראשי, ומתחתיו ההקשר (מתוכנן / שולם / כרטיסים / רווח צפוי).
    כך שמונה נתונים כספיים נקראים בראש מסך האירוע בלי לפתוח בלוק ובלי לגלול.
82. **אריח החישוב פותח את הבלוק שממנו המספר מגיע** (`openEventSection`) וגולל אליו.
    זו התשובה ל"ולא כל דבר צריכה לגולל" — ולכן **כל בלוק חדש חייב `sectionKey`**.
83. **הגוון נושא מידע, לעולם לא רק קישוט.** אותו כלל בבלוק הגיבור ובשורת אירוע:
    **כתום = תוך שבוע · כחול = עתידי · חול = כבר היה.** תמיד עם טקסט לצדו.
84. **טבעת המוכנות** היא `conic-gradient` על `--p`, ומציגה את אותו אחוז אמיתי של החלטה 59.
    **לא מוצגת על אירוע שעבר.**
85. **תוכן הוא לבן.** שם הוצאה, שם אדם, כותרת התראה וסכום — `#fff`.
    `--muted` הוא להקשר בלבד (הועלה ל-`#d3ccbf`), ו-`--faint` לרמז (`#b7b0a2`).
86. **אזור מגע נמדד, לא מחושב בראש.** `::after` שקוף ממוקם מול תיבת ה-padding:
    אלמנט 26px עם גבול 2px צריך `inset: -13px` כדי להגיע ל-48, לא `-11px`.
87. **`check-ui.js` בודק תחביר בלבד.** קוד שמתפרש עדיין יכול להתפוצץ בטעינה
    (למשל שימוש ב-`ICONS` לפני ההגדרה שלו). **חובה לפתוח את הקונסול בדפדפן אחרי כל מנה.**

### 2י. המערכת לא ממציאה — החלטה עקרונית (2026-09-20)
**זו ההחלטה החשובה ביותר בפרויקט נכון להיום. היא גוברת על כל שיקול מוצרי אחר.**

88. **אסור להציג פריט שהמערכת הסיקה לבד כאילו הוא משימה של המשתמשת.**
    לשון המשתמשת: *"דורש טיפול זה לא משימות שאני כתבתי. אלו משימות שהבינה שמה לי
    בלי לשאול אותי. לא מעוניינת."*
    `buildAttentionItems_` מחזיר שלושה סוגים; **רק `reminder`** (שורה ב-Notes שהיא הקלידה)
    מוצג כמשימה. `payment` ו-`readiness` אינם מוצגים בבית ולא במסך האירוע.
89. **הבלוק היחיד שמציג משימות נקרא "התזכורות שלי"** (`myRemindersHtml`), בבית ובמסך האירוע.
90. **מה שכן מותר להציג: סתירה בנתונים שהיא עצמה הזינה.** חפיפת שעות בין שני אמנים
    נשארת, תחת הכותרת "סתירה בנתונים" — זו עובדה, לא משימה שהומצאה.
91. **לא נמחקה שום יכולת.** `getAttentionItems`, `buildAttentionItems_` ומסך ההתראות
    המלא קיימים; המסך עבר להגדרות בשם **"התראות מערכת"**.
92. **שורת הפתיחה בבית לא סופרת "פעולות פתוחות".** היא מציגה תאריך ומספר אירועים פעילים.
93. **אריח ה-KPI "לטיפול" הוסר** והוחלף ב"תשלומים קרובים" — נתון עובדתי.

### 2יא. כרטיס האירוע (2026-09-20, אושר במפורש)
94. **הכרטיס הוא ערימה אנכית אחת**, לפי פריסה שהמשתמשת כתבה בעצמה:
    שם · מיקום · תאריך · עוד X ימים · רווח צפוי · כרטיסים שנמכרו · אמנים · לתשלום.
    הנימוק שלה: *"העין קוראת את זה הרבה יותר מהר. הבעיה היא שכולם מתחרים אחד בשני."*
    **אין יותר שם מול יתרה בשתי עמודות.**
95. **`getEvents` ו-`getInitialData` מחזירים `pnl` ו-`artists_count` לכל אירוע.**
    שינוי שרת שאושר במפורש. קריאה בלבד, בזיכרון, בלי קריאה נוספת ל-Sheets,
    בלי עמודה חדשה ובלי חישוב חדש (`computeEventPnl_` כבר קיימת). Helper: `eventCardMetrics_`.
96. **⚠ מספר האמנים נספר מההוצאות (`artist_id`), לעולם לא מהליינאפ.**
    `buildArtistLineup_` כולל רק אמנים **עם שעות הופעה**, ולכן אירוע עם שישה אמנים
    בלי שעות היה מציג "0 אמנים". נעול בבדיקה.
97. **מספר אף פעם לא מוצג בלי יחידה.** "150 כרטיסים" אסור — חייב "כרטיסים שנמכרו"
    או "320 מתוך 600". סכום שלילי מוצג כ"הפסד צפוי" עם ערך חיובי.
98. **הכלל הישן "הבית מחשב P&L רק לאירוע הראשון" בוטל** — הכרטיס צריך רווח צפוי בכל שורה.
    הבדיקה ב-`test-upgrade.js` עודכנה לשמור על מה שבאמת היה בסיכון: אפס קריאות נוספות ל-Sheets.

20. **Breakdown:**
    - `expensesBySubcategory` מהשרת. סכום תתי-הקטגוריות = סכום הקטגוריה.
    - Accordion לכל קטגוריה עם פירוט אמיתי.
    - השוואת אירועים נשארת כרגע ברמת קטגוריה.

---

## 3. כללים טכניים שחייבים לזכור

- **⚠ עמודה חדשה לטבלה קיימת — רק בסוף `SHEET_HEADERS`.**
  - `ensureSheetWithHeaders_` דורס את שורת הכותרות לפי מיקום, ו-`objectToRow_` כותב לפי הסדר הזה.
  - הוספה באמצע תזיז נתונים קיימים לעמודות הלא נכונות.
  - יש על זה בדיקה ב-`test-server.js`.
- **`updateRecord_`:** לא להעביר מפתחות עם `undefined`. זה דורס ערך קיים ל-''.
- **onclick ב-HTML:** להעביר מזהים (UUID) בלבד, לא שמות. גרש בשם שובר את מחרוזת ה-JS.
- **ערכים מה-Sheet:** Date עלול לחזור כאובייקט. תמיד דרך `rowToObject_` / `serializeCellValue_`.
- **ביצועים:**
  - בשרת: זיכרון לבקשה אחת בלבד (`REQUEST_MEMO_`). כתיבות קוראות מחדש בתוך ה-Lock.
  - **אסור** להוסיף מטמון בין בקשות שכתיבות נשענות עליו, כי זה דורס שינויים של המשתמש השני.
  - בלקוח: `ensureLookups()` + `LOOKUP_MUTATIONS`. פונקציית שרת חדשה שמשנה רשימת בחירה חייבת להתאים ל-Regex.

---

## 4. עבודה ופריסה
```bash
clasp push -f
```
```bash
clasp update-deployment AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ
```
- **בדיקות שרת:** שבע סוויטות — `test-server.js`, `test-coverage.js`, `test-stage0/1/2.js`,
  `test-upgrade.js` ו-`test-eventblocks.js` (V5: כל בלוק במסך האירוע מול השורות הגולמיות).
  עותק של `test-eventblocks.js` נשמר ב-`.ui-backup/` כדי שלא יאבד עם ה-scratchpad.
  כולן מריצות את `src/*.gs` מול Sheets מדומה, שמדמה גם המרת תאריכים של גוגל.
  - התצוגה המקומית `real.html` מריצה את השרת האמיתי בדפדפן.
  - הקבצים נמצאים ב-scratchpad של סשן 2026-09-14. בסשן חדש צריך לשחזר אותם (התיאור ב-WORKLOG).
- **הרגל עבודה:** מנות קטנות, QA בסוף כל מנה, ושינוי סכימה רק אחרי תוכנית מאושרת.
