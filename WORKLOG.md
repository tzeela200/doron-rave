# WORKLOG — שינויים מאז המסירה

## 2026-09-14 — קליטת החבילה + תיקוני שלב 1 (אמת עבודה, טרם נדחף ל-Apps Script)

### מבנה
- `src/` נוצר כהעתק של 13 קבצי הקוד + `appsscript.json` (Asia/Jerusalem, V8, Web App: Execute as me / Only myself).
- `doronsrave-package/` נשאר ללא שינוי כ-snapshot.

### תיקוני באגים ב-`src/` (לא שינוי החלטות)
| # | חומרה | בעיה | תיקון | קבצים |
|---|---|---|---|---|
| 1 | קריטי | Sheets ממיר `event_date` ל-Date. `google.script.run` לא מעביר Date, ולכן כל התשובה מגיעה ל-Frontend כ-null. הבית/אירועים/פרטי אירוע נופלים ל"אירעה שגיאה לא צפויה" ברגע שיש אירוע אחד. | המרת Date למחרוזת בקריאה (`rowToObject_`) | Utils.gs |
| 2 | גבוה | כתיבה ב-`appendRow` נותנת ל-Sheets לפרש ערכים: טלפון `0501234567` נשמר כמספר ומאבד את ה-0 | פורמט מפורש לכל שורה בכתיבה/עדכון + פורמט עמודות ב-`initializeSystem` (סעיף 6.4) | Config.gs, DataService.gs, SetupService.gs |
| 3 | בינוני | שם עם גרש (`DORON'S`, `Levi's`) שובר את ה-onclick ב-Autocomplete, והבחירה לא עובדת | מעבירים רק ID ל-onclick, השם נשלף מה-cache | JavaScript.html |
| 4 | בינוני | בחירת ספק ואז מעבר לקטגוריית "אמנים" (או להפך) השאירה את המזהה הישן, כך שנשמרו גם `vendor_id` וגם `artist_id`, והספק נספר ב-Breakdown לפי ספק | ניקוי המזהה מהסוג השני במעבר קטגוריה | JavaScript.html |
| 5 | בינוני | הקלדת שם בשדה "למי משלמים?" בלי בחירה מהרשימה נשמרה בשקט ללא ספק | הקלדה מבטלת בחירה קודמת; שמירה נחסמת עם הודעה | JavaScript.html |
| 6 | נמוך | כפתור "נסה שוב" במסך שגיאה של פרטי אירוע לא עבד (פונקציה אנונימית) | שמירת הפונקציה בזיכרון במקום לפי שם | JavaScript.html |
| 7 | נמוך | `new Date('yyyy-mm-dd')` מפוענח כ-UTC | פענוח כתאריך מקומי | JavaScript.html |

אחרי push צריך להריץ שוב `initializeSystem()` (אידמפוטנטי) כדי להחיל פורמט עמודות על Spreadsheet קיים.
טלפונים שכבר איבדו 0 לא משוחזרים אוטומטית.

### Push ראשון (2026-09-14)
- התברר שהפרויקט ב-Apps Script היה ריק: שלב 1 מעולם לא הותקן אצל המשתמשת, ולא נוצר Spreadsheet.
- `.clasp.json` חובר לפרויקט (`rootDir: src`). נדחפו 14 קבצים, ו-clone חוזר אימת ש-13 קבצי הקוד זהים ל-`src/`.
- שם הפרויקט: DORON'S RAVE. `initializeSystem()` רצה בהצלחה ב-11:58 ויצרה Spreadsheet
  (`1CdpmaJKGDLjOeHcWWVmQbalIE4zhNiPMQuxgTLBZubw`). ה-ID שמור ב-Script Properties; מתועד כאן לעיון בלבד, לא בקוד.
- פריסת Web App בוצעה (גרסה 1, "שלב 1", Execute as: Me, Only myself).
  Deployment ID: `AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ`
  URL: https://script.google.com/macros/s/AKfycby5WcKeZfs-vT-t-IjVQPxvLGN14cq3vqPpqSWJgWJNeogPW3TqybIF1XLGUljJA6ipTQ/exec
- **עדכון קוד בהמשך בלי לשנות URL:** `clasp push` ואז `clasp update-deployment <Deployment ID>`.
  (הפריסה `@HEAD` היא פריסת הבדיקה האוטומטית של גוגל, ולא הקישור הקבוע.)
- **QA שלב 1: עבר** (לפי דיווח המשתמשת, 2026-09-14).

## 2026-09-14 — עיצוב מחדש (בתהליך)
- המשתמשת: העיצוב הנוכחי "לא לטעמי". לוגו חדש: `logo.png` (שקוף). גרסה דחוסה לאפליקציה: `logo-app-q.png` (35KB).
- נמצא באג: כפתור ה-+ מופיע בראש המסך במקום בסרגל התחתון (`position` inline מול `top:-16px` במחלקה).
- נשלח לוח של 3 כיווני עיצוב. **המשתמשת בחרה A · "פוסטר לילה"**, והוא מחליף את החלטה #8 ב-HANDOFF (Navy + זהב).
- **Design System חדש (קנוני מעכשיו):** טוקנים בראש `Stylesheet.html`. שחור חם `#0c0b09`, שמנת `#efe4cc`, חול `#e4d0a8`,
  גחלת `#e39a6a` (לתשומת לב/באיחור בלבד), חמר `#d9826a` (שלילי), מרווה `#a9c08f` (חיובי).
  גופנים: Karantina (תצוגה/מספרים) + Heebo (טקסט), דרך Google Fonts. אייקוני SVG, בלי אימוג'י.
  תנועה: easing מותאם ו-stagger בכניסה. `prefers-reduced-motion` נתמך.
- כפתור ה-+ הועבר לתוך הסרגל התחתון (תוקן הבאג). הלוגו החדש מוטמע ב-`Index.html` (35KB).
- נבדק בתצוגה מקומית עם נתוני דמה, מחוץ ל-Sheet: בית, אירועים, פרטי אירוע, טופס הוצאה, הגדרות. בלי גלילה אופקית ב-390px.
- נדחף ועודכן על אותו קישור: פריסה @2 "עיצוב פוסטר לילה".
- `brand/`: תמונת תצוגה מקדימה לקישור (1200×630) ואייקונים (512, 180).
  מגבלה: קישור של Apps Script מציג תצוגה מקדימה של גוגל, לא שלנו (ראה הסבר בשיחה).

### החלטה: "מועד תשלום" בטופס ההוצאה — הוכרעה (אפשרות 1)
- בטופס הראשי, "מועד תשלום", "איך שולם?" והסטטוס "שולם" מנהלים **תשלום יחיד**. זה חל רק ב"מצב פשוט":
  אין תשלומים, או יש תשלום אחד שסכומו שווה לסכום ההוצאה.
- בפיצול (כמה תשלומים או מקדמה חלקית) הטופס הראשי לא נוגע בתשלומים, והניהול עובר ל"פרטים נוספים ← תשלומים ומקדמות".
- `expense_date` חזר למשמעות שלו לפי האפיון: "תאריך" תחת "פרטים נוספים".

## 2026-09-14 — Payments + Income + איזון עיצוב (נדחף, פריסה @3)
- **חדש:** `PaymentsService.gs`, `IncomeService.gs`. Events, Expenses ו-Code עודכנו לחישוב שולם / נותר / סטטוס מחושב / באיחור.
- **UI:**
  - תשלומים ומקדמות עם עורך מוטמע בתוך טופס ההוצאה.
  - הכנסות עם סה"כ שמתעדכן בזמן הקלדה.
  - "תשלומים קרובים": בבית ובמסך נפרד. באיחור מסומן בגחלת.
  - Planned vs Actual.
  - בבית נוסף אזור "ניהול" עם כל פריטי ההגדרות (בקשת המשתמשת).
- **עיצוב מאוזן** (המשתמשת: "גדול מדי"):
  - סולם טיפוגרפי עברי 13–30px לפי hebrew-tailwind-preset, ותצוגת Karantina עד 52px.
  - ₪ קטן בגופן הגוף.
  - logical properties בכל המקומות.
  - Tailwind עצמו **לא** הוכנס: הוא דורש build או Play CDN, וזה סותר את החלטה #2. יושמו העקרונות של הסקיל ב-CSS הקיים.
- **אימות:**
  - `test-server.js` (scratchpad): הקוד האמיתי של השרת רץ מול Sheets מדומה שממיר תאריכים כמו גוגל. 33/33 עברו, כולל תרחישי QA 92, 93 ו-95.
  - תצוגה מקומית שמריצה את השרת האמיתי בדפדפן. נבדקו דרך ה-UI: עריכת תשלום ← שולם; מניעת לחיצה כפולה (תשלום והכנסה); מצב פשוט ← שולם + אמצעי תשלום; הכנסה 300×50; רענון סיכומים.
  - **בטלפון עצמו עוד לא נבדק.**
- **עוד לא נבנה:** ניהול אמצעי תשלום, כפתור + כפעולה מהירה (הוצאה/הכנסה מחוץ לאירוע), Breakdown לפי ספק, שכפול, השוואה, חיפוש/סינון, קבצים מצורפים.

## 2026-09-14 — שני משתמשים + שכפול + השוואה + חיפוש + אריחים (נדחף, פריסה @4)
- **קישור קצר** (באישור המשתמשת): https://tinyurl.com/doronsrave, מפנה לכתובת ה-Web App.
- **שני משתמשים (המשתמשת + דורון)**, שינוי מכוון מהגדרת הפריסה הקודמת "רק אני":
  - `appsscript.json`: `executeAs: USER_ACCESSING`, `access: ANYONE` (כל מי שמחובר עם חשבון Google).
  - **הרשאה בפועל = שיתוף קובץ ה-Sheets כעורך.** מי שאין לו גישה לקובץ לא מקבל נתונים (Google אוכפת), ורואה הודעה ברורה.
  - אין רשימת משתמשים בקוד. הוספה או הסרה של משתמש נעשית בשיתוף הקובץ.
  - מסך ההגדרות מציג עם איזה חשבון מחוברים.
- **שכפול אירוע** (`CloneService.gs`): "הכל כבסיס" / "מבנה בלבד" + בחירת הוצאות.
  תשלומים משוכפלים לעולם לא "שולם", ומועדי התשלום זזים לפי ההפרש בין תאריכי האירועים.
- **השוואת אירועים** (`ReportsService.gs`): טבלה + גרף עמודות הכנסות/הוצאות. קטגוריה חסרה = 0.
- **חיפוש וסינון**: טקסט + 11 מסננים ב-Bottom Sheet, צ'יפים של מסננים פעילים, סיכום תוצאות.
- **ניהול אמצעי תשלום**: הוספה, עריכה, השבתה. בלי מחיקה.
- **כפתור +** מחוץ לאירוע: אירוע / הוצאה / הכנסה, עם בחירת אירוע.
- **Breakdown לפי ספק**: מתג בפאנל "חלוקת הוצאות".
- **עיצוב v3** (המשתמשת: "יותר מדי שחור", "הפרדה לא בולטת", "ריבועים בבית"):
  - רקע כחול-לילה `#141a24`.
  - פאנלים צבעוניים: הוצאות = חמר, הכנסות = מרווה, תשלומים = גחלת, אירועים = כחול.
  - 8 אריחים צבעוניים בבית.
- **אימות:**
  - `test-server.js`: כל הבדיקות עברו, כולל QA שכפול 3,000→3,500 (המקור נשאר 3,000).
  - תצוגה עם השרת האמיתי: שכפול (כולל לחיצה כפולה), השוואה, חיפוש "באיחור", אמצעי תשלום, תפריט +, אריחים, בלי גלילה אופקית.
- **לא נבדק:**
  - הגישה של דורון בפועל. תלויה בחשבון שלו ובאישור ההרשאות בכניסה הראשונה.
  - בטלפון.
- **עוד לא נבנה:** קבצים מצורפים (Drive), ליטוש נגישות סופי, ניקוי נתוני בדיקה לפני מסירה (סעיף 96).

## 2026-09-14 — ייבוא רשימות (פריסה @5)
- `importArtists` / `importVendors` + כפתור "ייבוא רשימה": שם בכל שורה, מזהה אוטומטי, דילוג על שמות קיימים.
- **נבדק:** 10 השמות של המשתמשת נכנסו בתצוגה המקומית, ולחיצה כפולה לא יצרה כפילות.
- **לא בוצע:** הייבוא לנתונים האמיתיים. ממתין להחלטת המשתמשת: היא מדביקה, או ביצוע דרך הדפדפן שלה.

## 2026-09-14 — קטגוריות ותתי-קטגוריות (תוכנית אושרה; פריסה @6)
- **תוכנית:** `~/.claude/plans/piped-booping-fern.md`. ההחלטות מתועדות ב-`HANDOFF.md`, סעיף 2א.
- **סכימה:**
  - טבלה חדשה `Subcategories`.
  - `Expenses.subcategory_id` **בסוף** הטבלה.
  - `ensureSubcategorySchema_()` יוצרת את שניהם בשימוש הראשון.
- **שרת:**
  - `saveSubcategory` / `archiveSubcategory` / `seedCategoryTaxonomy`.
  - `getCategories` מחזיר גם `subcategories`.
  - `resolveSubcategory_`: ריק מותר, "אמנים" מאופס, תת-קטגוריה חייבת להתאים להורה.
  - `expensesBySubcategory`, שכפול מעתיק `subcategory_id`, מסנן חיפוש `subcategory_id`.
- **UI:**
  - שדה "תת-קטגוריה (לא חובה)" עם Autocomplete ויצירה תוך כדי מילוי. לא מופיע באמנים.
  - Accordion בחלוקת הוצאות.
  - ניהול תתי-קטגוריות במסך הקטגוריות.
  - מסנן תלוי-קטגוריה בחיפוש.
- **אימות:**
  - `test-server.js`: 84/84.
    - מיגרציה: גיליון ישן עם נתונים מקבל את העמודה בסוף, בלי הזזת ערכים.
    - seed אידמפוטנטי: 6 קטגוריות ו-36 תתי-קטגוריות, והרצה שנייה = 0.
    - אמנים מאופס. תת-קטגוריה זרה נדחית.
    - אין Double Counting.
    - שכפול, חיפוש, ארכוב עם שם היסטורי שנשמר, ואין מחיקה פיזית.
  - תצוגה מקומית עם השרת האמיתי, דרך ה-UI:
    - בחירת קטגוריה ← תת-קטגוריה ← יצירה תוך כדי ← שמירה.
    - מעבר לאמנים מסתיר את השדה ומציג "אמן".
    - החלפת קטגוריה מנקה את תת-הקטגוריה.
    - חסימת טקסט שלא נבחר.
    - Accordion.
    - מסך קטגוריות.
    - בלי גלילה אופקית ב-390px.
- **תיקון בתוכנית:** "שונות" מופיעה תחת **4** הורים, לא 5. בתשתית זה "אחר", וב"בלתי צפוי" אין.
- **המשתמשת צריכה להריץ פעם אחת מהעורך:** `seedCategoryTaxonomy`.
- **תיעוד:** `HANDOFF.md` חדש בשורש; `CLAUDE.md` מפנה אליו.

## 2026-09-14 — מהירות (פריסה @7)
- **חבילה חיצונית `Downloads/filesdoron3.zip` — לא הותקנה.** היא בנויה על קוד שלב 1 הישן:
  - JavaScript של 49KB מול 117KB היום.
  - מחזירה את `appendRow` (באג ה-0 בטלפון) ומוחקת את המרת התאריכים (הבאג הקריטי של null).
  - מוחקת תשלומים, עיצוב, שני משתמשים ותתי-קטגוריות.
  - ה-CacheService שבה גם קורא נתונים לכתיבה מתוך מטמון של עד 60 שניות, ועלול לדרוס שינוי של משתמש אחר.
  - הרעיונות שבה נכונים בכיוון, ויושמו מחדש בצורה בטוחה על הקוד הנוכחי.
- **שרת:** זיכרון לבקשה אחת בלבד (`REQUEST_MEMO_` ב-Utils.gs):
  - openById פעם אחת לבקשה.
  - `getDataRange` אחד לכל Tab, משותף לכל הפונקציות.
  - כתיבות קוראות מחדש בתוך ה-Lock ומבטלות את הזיכרון.
  - אין מטמון בין בקשות, ולכן אין מידע ישן.
- **שרת:** `getFormLookups()` (כל רשימות הבחירה בקריאה אחת), `getCurrentUser()`. `getInitialData` מחזיר קטגוריות עם תתי-קטגוריות.
- **לקוח:**
  - `ensureLookups()` שומר את הרשימות בזיכרון.
  - כל save/archive/import של רשימה מסמן אותן כלא-עדכניות (`LOOKUP_MUTATIONS` ב-`callServer`).
  - עריכת אירוע משתמשת בנתוני המסך. ההגדרות לא טוענות את כל הבית.
- **מדידה** (`measure.js`, פעולות Sheets לבקשה, 20 הוצאות):
  - בית: 39 → 19.
  - פרטי אירוע: 45 → 19.
  - שמירת הוצאה: 12 → 7.
  - פתיחת טופס הוצאה: 4 בקשות / 35 פעולות → 0 בקשות (אחרי טעינה ראשונה), או בקשה אחת / 11 פעולות.
- **אימות:**
  - כל הבדיקות עוברות, וכל קריאה מדמה עכשיו הרצה נפרדת.
  - תצוגה עם השרת האמיתי, ספירת בקשות בפועל:
    - טופס הוצאה, עריכת אירוע והגדרות: 0 בקשות.
    - אחרי שמירת ספק: בקשה אחת, והספק מופיע.
    - שמירת הוצאה + תת-קטגוריה עובדת, והלחיצה הכפולה נחסמה.
- **לא נעשה (אפשרות להמשך):** קריאה אחת לכל הטבלאות דרך Advanced Sheets API (`batchGet`), או CacheService רק לרשימות.

## 2026-09-14 — סגירת פתוחים
- **לפי דיווח המשתמשת:** `seedCategoryTaxonomy()` הורצה, ו-10 אמני ה-DJ יובאו דרך "ייבוא רשימה" לנתונים האמיתיים.
- **הבהרה:** לא אומת על ידי ישירות מול ה-Sheet.

## 2026-09-15 — עיצוב נגיש v4 (פריסה @8)
- **המשתמשת:**
  - הכתב קטן מאוד.
  - הגופן לא לטעמה.
  - העיצוב צפוף.
  - 0 ו-8 נראים אותו דבר.
  - ביקשה עיצוב מותאם ונגיש לפי skill `israeli-ui-design-system`.
- **שינויים** (שכבת עיצוב בלבד, בלי שינוי לוגיקה):
  - Rubik במקום Karantina + Heebo (הצמד "Friendly Startup" מהסקיל), ספרות `tabular-nums`.
  - סולם: 14/15/17/19px לטקסט, 20/24/28/40px לכותרות ומספרים. line-height 1.3 לכותרות, 1.7 לגוף.
  - הוסר כל letter-spacing. נוסף word-spacing 0.05em.
  - מרווחים: פאנלים, שורות, טפסים ואזורי מגע 48–58px. אריחים 2 בשורה (4:3) במקום 3.
  - ניגודיות: טקסט משני `#c2baab` / `#aaa293`. סימן ₪ ב-0.72em.
  - סכומים בשורת 3 עמודות: 20px בלי שבירת שורה (תוקן מקרה שבו ה-₪ ירד לשורה נפרדת).
- **אימות** (בדיקת DOM בתצוגה עם השרת האמיתי, 390px, 7 מסכים: בית, אירוע, טופס הוצאה, קטגוריות, תשלומים, השוואה, חיפוש):
  - Rubik נטען.
  - גופן מינימלי 14.5px (סימן ₪). כל השאר ≥ 14.9px.
  - 0 אלמנטים עם letter-spacing.
  - בלי גלילה אופקית.
  - **לא נבדק בטלפון.**

## 2026-09-15 — גופן Heebo (פריסה @9)
- **החלטה:** המשתמשת בחרה Heebo במקום Rubik, מתוך שתי החלופות שהוצעו (Heebo / Assistant).
- **מה השתנה:**
  - הוחלפו רק הטוקנים `--font-display` / `--font-hebrew` וטעינת הגופן ב-Index.html.
  - הסולם, המרווחים והניגודיות של v4 לא השתנו.
- **אימות** (390px, 5 מסכים: בית, אירוע, קטגוריות, השוואה, חיפוש):
  - Heebo נטען.
  - אף סכום לא נשבר לשתי שורות.
  - בלי גלילה אופקית.

## 2026-09-15 — היררכיית קטגוריות ונוחות (פריסה @10)
- **המשתמשת:** "רוצה נוח", הבדל ברור בין קטגוריה לתת-קטגוריה. הרשתה לשנות צבע ומבנה עיצובי.
- **מסך קטגוריות:**
  - כל קטגוריה היא כרטיס: אייקון תיקייה, שם 19px מודגש, תג "N תתי-קטגוריות", חץ מסתובב.
  - עריכה = אייקון עיפרון 48px.
  - תתי-קטגוריות באזור שקוע: רקע כהה, כותרת "תתי-קטגוריות של X", קו-ענף, טקסט 17px רגיל, כפתור "+ תת-קטגוריה חדשה" מקווקו.
  - אמנים: כרטיס בגוון חמר עם מיקרופון וקישור לרשימת האמנים.
  - שורת הסבר בראש המסך.
  - נגישות: `aria-expanded` / `aria-controls` / `aria-label`.
- **טופס הוצאה:** שדה "תת-קטגוריה של {קטגוריה} (לא חובה)" מוזח, עם קו התחלה.
- **שורת הוצאה:** "ציוד טכני ← הגברה · ספק".
- **חלוקת הוצאות:** חץ SVG מסתובב. פירוט באזור שקוע עם פס כחול וכותרת "פירוט לפי תת-קטגוריה".
- **אימות** (תצוגה עם השרת האמיתי, 390px):
  - 8 כרטיסים, פתיחה/סגירה עובדת, בלי גלילה אופקית.
  - צילומי מסך של רשימת הקטגוריות, כרטיס פתוח וטופס הוצאה.
  - תחביר JS תקין.
  - **לא נבדק בטלפון.**

## 2026-09-15 — נוחות ומקצועיות (פריסה @11)
- **המשתמשת:** "תתקן ותעשה את כל מה שצריך שיהיה יותר קל, נוח, מקצועי ויפה".
- **ממצאי סקירה ותיקונים:**
  1. **לא היה כפתור חזרה:**
     - נוסף `back-btn` בכותרת + `navStack` / `goBack()`.
     - טאבים בסרגל התחתון, הלוגו וארכוב אירוע מאפסים את ההיסטוריה.
     - אחרי יצירה או שכפול, החזרה מדלגת על טפסי התהליך.
  2. **כפתור שמירה נעלם בטפסים ארוכים:** `position: sticky` לכפתור ה-submit בכל Bottom Sheet.
  3. **שדות סכום בלי ₪:** `.money-input` עוטף סכום הוצאה, סכום מתוכנן, סכום תשלום ומחיר ליחידה בהכנסה.
  4. **ספקים/אמנים:** `personCardHtml`: עיגול עם אות ראשונה (כחול לספק, חמר לאמן), טלפון, "N אירועים · סכום", הערות מערכת של אמן.
  5. **מסך אירועים:** כרטיסים (`.card-list`).
  6. **ויזואלי:** הוסרה טקסטורת הגרעיניות. אנימציית כניסה קוצרה מ-0.6s ל-0.28s. כפתור ⋮ הוגדל ל-48px.
- **באג שנתפס בבדיקה ותוקן לפני הפריסה:**
  - סקריפט העטיפה הוסיף רק `</div>` סוגר בלי פותח בשדות הסכום, מה שעלול לשבור את מבנה הטופס.
  - תוקן ואומת ב-DOM: הקלט עטוף, ה-form-group וה-form-row שלמים.
- **אימות** (תצוגה עם השרת האמיתי, 390px):
  - ניווט: בית ↔ קטגוריות. הגדרות ← ספקים ← חזרה = הגדרות. יצירת אירוע ← חזרה = אירועים.
  - כרטיסי ספקים ואמנים.
  - ₪ ב-4 השדות.
  - כפתור שמירה נראה בלי גלילה בטופס של פי 2 מגובה המסך.
  - בלי גלילה אופקית. בדיקות השרת עוברות.
  - **לא נבדק בטלפון.**

## 2026-09-16 — שדרוג לפי DORONS_RAVE_FINAL_CLAUDE_PROMPT.md (נבנה, נבדק ונפרס @12)
- **חדש:** `SchemaService.gs`, `NotesService.gs`.
- **סכימה (הכול בסוף הטבלאות):** Notes (Tab חדש), Events +2, Artists +2, Expenses +2, Income +1.
- **שרת:** `computeEventPnl_`, `buildArtistLineup_`, `ticketForecastFields_`, `resolvePerformanceTimes_`, `performanceInfo_`,
  `calculatePerformanceDuration_`, `parseTimeToMinutes_`, `nightSortKey_`, `artistDisplayName_`, `upgradeSchema`.
  `ensureSubcategorySchema_` הפך למעטפת ל-`ensureEnhancementSchema_` (בלי מנגנון מקביל).
- **UI:** פאנל "תמונה פיננסית" עם נקודת איזון ופס התקדמות, "לוח אמנים" עם אזהרת חפיפה, "הערות ותזכורות",
  שעות הופעה בטופס ההוצאה עם משך ועלות לשעה חיים, שם במה/שם אמיתי + הערות אמן, checkbox כרטיסים בהכנסה,
  תחזית כרטיסים בטפסי אירוע ושכפול, ושורה אחת קצרה בבית.
- **אימות:**
  - `test-upgrade.js` חדש: 60 בדיקות. כולל שדרוג סכימה על גיליון ישן עם נתונים (אף ערך לא זז), עצירה בשגיאה על סדר עמודות שגוי,
    חישובי משך (60/120/90), סדר לוח חוצה חצות, חפיפות, בידוד הערות בין אירוע לאמן, ו-P&L מקרים 1–4.
  - `test-server.js`: כל הבדיקות הקיימות עוברות (שתי הנחות ישנות על מיקום העמודה עודכנו).
  - בדיקת UI מול השרת האמיתי: 14 מסכים, 6 Bottom Sheets, בלי שגיאות JS ובלי גלילה אופקית.
  - תוקן בבדיקה: שורת לוח האמנים נדחסה — הוחלפה ל-Grid, אזהרת חפיפה בשורה מלאה.
- **פריסה (באישור המשתמשת, 2026-09-16):** `clasp push -f` — 20 קבצים; `clasp update-deployment AKfycby5...QA6ipTQ` — גרסה @12, אותו קישור.
  שדרוג הסכימה רץ אוטומטית בכתיבה הראשונה; אפשר גם `upgradeSchema()` ידנית מהעורך.

## 2026-09-16b — שדרוג מוצרי לפי "אפיון שדרוג מלא למערכת הקיימת" (P0+P1, נבנה, נבדק ונפרס @13)
- **חדש:** `CoverageService.gs`, `AttentionService.gs`.
- **סכימה:** `ExpenseCoverage` בלבד (Tab חדש). **אפס עמודות חדשות לטבלאות קיימות.** `SCHEMA_VERSION` → `2026-09-16b-expense-coverage`.
- **תיקון באג אמיתי:** `getInitialData` מיין יורד ולקח את הראשון — כלומר הבית הציג את האירוע הרחוק ביותר.
  עכשיו: עתידיים בלבד, ממוין עולה. אותו תיקון הוחל גם על `getEvents`.
- **שרת:** `buildCoverageOptions_`, `resolveCoverage_`, `coverageMapByExpense_`, `normalizeCoverageItems_`,
  `buildOperationalCoverage_`, `buildAttentionItems_`, `listOpenReminders_`, `summarizeActiveFinances_`,
  `nextEventForExpenses_`, `isVenueSubcategoryName_`, `getAttentionItems`.
- **UI:** דשבורד בבית (הפוסטר נשמר, הוקטן), פעולות מהירות, "דורש טיפול" בבית ובאירוע ובמסך מלא,
  מתוכנן/מוסכם/שולם/נותר, "מה סגור באירוע", מתג "האם המחיר כולל?" עם צ'יפים בטופס ההוצאה,
  סכום מתוכנן יצא מ"פרטים נוספים" לאזור הכספי, שורת סוכם/שולם/נותר גלויה, והקשר בכרטיסי ספק/אמן.
- **אימות:**
  - `test-coverage.js` חדש: 76 בדיקות. שדרוג סכימה בלי לגעת בנתונים, אירוע קרוב מול מאוחר,
    כיסוי שלא נספר כספית (סכומי קטגוריה זהים לפני ואחרי), ארכוב והחייאה בלי כפילות,
    דחיית כיסוי שגוי בלי כתיבה, "דורש טיפול", סיכום כספי, שכפול, וכרטיסי ספק.
  - `test-server.js` ו-`test-upgrade.js`: עוברים (עודכנה הנחה אחת על מבנה `getInitialData`).
  - בדיקת UI מול השרת האמיתי: 12 מסכים + טפסים, בלי שגיאות JS ובלי גלילה אופקית.
    ב-390px הכפתורים גולשים לשתי שורות; ב-320px נגללים אופקית; 55px גובה ו-18px טקסט בשניהם.
  - **תוקן בבדיקה:** `getInitialData` סימן את המטמון כטעון בלי להחזיר `coverageOptions`, ולכן הצ'יפים היו ריקים.
    התיקון: הבית מחזיר גם אותם, והצ'יפים מציגים תמיד את מה שכבר מסומן.
  - **תוקן בבדיקה:** הוצאה במצב "מתוכנן" עם סכום הוצגה כ"מכוסה" במקום "בתהליך".
- **פריסה (באישור המשתמשת, 2026-09-16):** `clasp push -f` — 22 קבצים (2 חדשים); `clasp update-deployment AKfycby5...QA6ipTQ` — גרסה @13, אותו קישור.
  `ExpenseCoverage` ייווצר בכתיבה הראשונה, או ידנית דרך `upgradeSchema()`.

## 2026-09-16c — שלב 0: תיקוני אמת מהנתונים החיים (נפרס @14)
שלושת הממצאים מהצילומים של המשתמשת באפליקציה החיה (@13).

1. **"מה כלול במחיר" לא היה נגיש.** `VENUE_SUBCATEGORY_NAMES` חיפש `'השכרת שטח'` במחרוזת מדויקת,
   ואצל המשתמשת הקטגוריה נקראת "שכירות מקום". **תוקן:** המתג מוצג בכל הוצאה, ברירת מחדל "לא".
   הקבועים `VENUE_SUBCATEGORY_NAMES` / `VENUE_SUBCATEGORY_NAMES_UI` ו-`isVenueSubcategoryName_` הוסרו.
   ההוצאה מוחרגת מרשימת הרכיבים של עצמה (`ownKey` בצד הלקוח).
2. **"מתוכנן" ו"מוסכם" הציגו את אותו מספר.** הטופס דרש "סכום מוסכם" גם בסטטוס "מתוכנן".
   **תוקן בטופס:** `renderAmountFieldHtml()` — השדה הראשי והנדרש עוקב אחרי הסטטוס, והראשי מוצג ראשון.
   נוספה הצעה מפורשת בלחיצה אחת (`moveAmountToPlanned` / `moveAmountToAgreed`) לסכום שנמצא בשדה הלא נכון.
3. **"דורש טיפול" היה ריק כשכלום לא היה מסודר.** הוא קרא רק שורות Payments/Notes קיימות.
   **תוקן:** `addReadinessItems_` מוסיף פריטים נגזרים לאירוע בטווח `EVENT_READINESS_HORIZON_DAYS` (30 יום):
   הוצאות שלא סוכמו (= ספק/אמן שטרם אישר), עסקאות שסוכמו בלי מועד תשלום, אין מחיר כרטיס, אין הכנסות,
   ואירוע בלי אף הוצאה. מקובץ לשורה אחת לאירוע לכל סוג, כדי שהרשימה תישאר קצרה.

**נדחה ובוטל תוך כדי:** ניסיתי לתקן את (2) גם בשרת (`resolveAmountsByStatus_`), והוסר.
ברירת המחדל של `normalizeManualStatus_` היא "מתוכנן", ולכן כל קורא שלא שולח סטטוס — ייבוא, שכפול, API —
היה מאבד את `agreed_amount` בשקט. התיקון שייך לטופס, שם נוצרה הבעיה. `test-stage0.js` נועל את הכלל הזה.

- **אימות:** `test-stage0.js` חדש (30 בדיקות) + `test-server.js` / `test-upgrade.js` / `test-coverage.js` — הכול עובר.
  ב-`test-coverage.js` עודכנו שלוש הנחות שנשענו על הזיהוי לפי שם; בדיקת הארכוב חודדה כך שהיא בודקת
  את הכיסוי של ההוצאה שארוכבה עצמה, ולא "אין אף כיסוי באירוע".
- **בדיקת UI מול השרת האמיתי:** 12 מסכים, בלי שגיאות JS ובלי גלילה אופקית. אומת בדפדפן:
  המתג מופיע על קטגוריה רגילה; "סכום מתוכנן" ראשון ונדרש בסטטוס מתוכנן ומתהפך ב"סוכם";
  ההצעה להעביר סכום עובדת מקצה לקצה (2,500 ₪ עברו ל-`planned_amount` ו-`agreed_amount` התאפס),
  ומאז סה"כ מוסכם 39,000 ₪ מול מתוכנן 42,500 ₪ — שני מספרים נפרדים.
- **נפרס @14** (באישור המשתמשת, 2026-09-16), אותו קישור.

## 2026-09-16d — שלב 1: מסך הבית הופך למרכז שליטה (נפרס @14)
לפי פידבק היועץ: המערכת הייתה פסיבית — הציגה מידע במקום לומר מה לעשות.

- **"היום" — מרכז משימות בראש המסך.** כל שורה מנוסחת **בציווי ועם שם האדם**:
  "לשלם לאלעד" · "לסגור עם אמיר" · "לקבוע מועד תשלום לSquid" · "חסרים 180 כרטיסים לנקודת האיזון" ·
  "בעוד 2 ימים אירוע: ערב קרוב". כשאין אדם מאחורי ההוצאה הניסוח משתנה בהתאם
  ("לשלם עבור שונות", "לסגור את גדרות") — עברית נכונה, לא תבנית אחת לכל המקרים.
- **עד שלושה בשמם, אחר כך מקובץ.** "לסגור עם אמיר" שווה הרבה יותר מ-"3 הוצאות לא סוכמו",
  אבל שבע שורות כאלה הן רעש. `namedOrGrouped_()`.
- **4 KPI:** אירועים פעילים · נותר לתשלום · רווח צפוי · דורש טיפול. כל אחד לחיץ ומוביל למסך הרלוונטי.
  "רווח צפוי" משתמש בתחזית הכרטיסים כשהיא קיימת, ואחרת נופל למאזן בפועל (`forecast_is_projection`).
- **סדר חדש:** לוגו ← ברכה ← "היום" ← KPI ← פוסטר ← פעולות מהירות ← אירועים ← תשלומים ← ניהול ← היסטוריה.
- **הפוסטר נשאר** (הכרעת המשתמשת), אבל יורד מתחת למשימות ול-KPI.
- **ברכה בלי שם** — לאפליקציה שני משתמשים, וברכה בשם הלא נכון גרועה מברכה בלי שם.
- **שפת משימות אחת בכל המסכים:** `taskRowHtml` החליף את `attentionRowHtml` גם במסך האירוע וגם במסך המלא.
  נמחקו `attentionPanelHtml`, `attentionRowHtml`, `financialPanelHtml` — אין להם יותר קוראים.

**מה ירד לפי הפידבק:** אין Tab `EventNeeds` (הרשימה תהיה קבועה בקוד + עמודה אחת בסוף `Events`),
אין גרפים, אין Timeline, ו**אין שפת צבעים חדשה** — רק הטוקנים הקיימים וצבעי סטטוס.

- **אימות:** `test-stage1.js` חדש (22 בדיקות) + ארבע הסוויטות הקיימות — הכול עובר.
  בדפדפן ב-375px: **4 המשימות ו-KPI הראשון נראים בלי לגלול**; ב-320px בלי גלילה אופקית ואזורי מגע 86px.
  12 מסכים, אפס שגיאות JS.
- **תוקן תוך כדי:** `whoName` היה מקומי ל-`buildAttentionItems_` ושבר את `getEventDetails` — מועבר כפרמטר.
  הלוגו צומצם (בלע רבע מהמסך הראשון), והתאריך המדויק ירד מהשורה כשכבר כתוב "באיחור"/"השבוע".
- **נפרס @14** (באישור המשתמשת, 2026-09-16), אותו קישור.

## 2026-09-16e — שלב 2: מסך האירוע הופך למסך תפעולי (נפרס @15)
מתחילים באירוע, לא במספרים.

- **"25 ימים לאירוע" כמספר הגדול היחיד** בראש המסך. היתרה ירדה ל"תמונה פיננסית".
- **מצב ההפקה:** רשימה קבועה של 11 שלבים (`EVENT_REQUIREMENTS` ב-`Config.gs`) עם ארבעה מצבים נגזרים:
  ✓ מכוסה · ✓ כלול ב-X · • בתהליך · ⚠ חסר. לכל שורה כפתור "לא צריך" / "להחזיר".
- **"זה שלא צריך זה לא אומר שזה חסר"** (הכלל של המשתמשת): מה שסומן "לא צריך" יוצא מהחוסרים
  ומהמונה, ונאסף לאזור מקופל. **אין Tab חדש** — נשמרים רק החריגים בעמודה `Events.skipped_requirements`,
  שנוספה **בסוף** הטבלה. אירוע בלי אף סימון מתנהג כאילו כל הרשימה נדרשת.
- **Accordion:** הוצאות · הכנסות · תשלומים · אנשים וספקים · חלוקת הוצאות · הערות · תמונה פיננסית.
  הכול סגור, **אחד נפתח בכל פעם**, והסיכום בכותרת נשאר גלוי גם כשסגור. `collapsiblePanelHtml()`.
- **פאנלים חדשים:** "תשלומים" מרכז את כל תשלומי האירוע בלי להיכנס להוצאה;
  "אנשים וספקים" נגזר מההוצאות **בלי לאחד טבלאות**.
- **סדר:** אירוע ← ספירה לאחור ← מצב ההפקה ← לוח אמנים ← דורש טיפול ← פעולות מהירות ← Accordion.

**תוקן בבדיקה בדפדפן (שני באגים אמיתיים):**
1. **"אמנים" הופיע כ-⚠ חסר** למרות שני אמנים מוזמנים — `buildOperationalCoverage_` דילג בכוונה על
   קטגוריית אמנים (שריד מהתקופה שבה הוא שירת רק את פאנל הכיסוי). ההחרגה הוסרה.
2. **רכיב נספר פעמיים** — "השכרת שטח" הופיע גם כ"✓ מקום" וגם ב"סגור גם". נוסף `matched_name`
   לכל שורת היערכות, והלקוח מדלג על מה שכבר נספר.

- **נמחק:** `coveragePanelHtml`, `pnlPanelHtml` ו-`notesPanelHtml` (השניים האחרונים הפכו ל-`*BodyHtml`).
- **אימות:** `test-stage2.js` חדש (23 בדיקות) + חמש הסוויטות הקיימות — הכול עובר.
  כולל: העמודה נוספת בסוף ושורת אירוע קיימת לא זזה; סימון כפול לא מכפיל; מפתח לא מוכר נדחה;
  אירוע אחר לא מושפע; וסימון "לא צריך" לא משנה שום סכום.
  בדפדפן: 12 מסכים, אפס שגיאות JS, בלי גלילה אופקית, ומחזור מלא של "לא צריך" ← "להחזיר".

## 2026-09-16f — שלב 1b: לראות את כל העסק, לא אירוע אחד (נפרס @16)
לפי פידבק היועץ על מסך הבית. הוא הגיב על צילום מלפני @14, ולכן ארבע משש השאלות שלו
("כמה אירועים פעילים", "כמה חייב", "כמה ארוויח", "מה בוער") כבר נענו שם. שתי ההערות שנשארו נכונות טופלו כאן.

- **אחוז מוכנות לכל אירוע.** `summarizeEventReadiness_()` מחזיר `{done, total, missing, percent}`
  לכל אירוע עתידי. מוצג בפוסטר ("36% מוכן · 6 דברים פתוחים") ובכל שורת אירוע ("9% · 1 מתוך 11 סגורים").
  **זה נתון אמיתי משלבי ההפקה של שלב 2, לא ציון שהומצא** — היועץ עצמו פסל אחוז מוכנות
  "עד שלא קיימת מערכת אמיתית שמחשבת אותו", והמערכת הזו נבנתה בשלב 2.
  כך אפשר לראות במבט אחד **באיזה אירוע מפגרים** — השאלה שנשארה פתוחה.
- **כל האירועים העתידיים בבית**, לא שניים.
- **אזור הניהול הפך לרשימה מקופלת.** קודם היה גריד של 8 אריחים שתפס חצי מסך — תפריט, לא מידע ניהולי.
- **אירוע שעבר לא מציג מוכנות** — אין לאן להיערך.

**Refactor:** `buildOperationalCoverage_` עבר לקבל שורות גולמיות + מפות + `paidExpenseIdSet_`,
במקום הוצאות מועשרות. כך אותו קוד בדיוק משרת גם את מסך האירוע וגם את סיכום הבית,
בלי להריץ העשרה מלאה לכל אירוע ובלי לשכפל את הכלל מתי עסקה נחשבת סגורה.

- **אימות:** שש הסוויטות עוברות; `test-stage1.js` הורחב ב-7 בדיקות מוכנות, כולל שסימון "לא צריך"
  מקטין את המכנה. בדפדפן ב-375px וב-320px: אפס שגיאות JS, בלי גלילה אופקית, שורות ניהול 52px.

## 2026-09-17 — עיצוב מחדש מלא של שכבת הממשק (נפרס @17)
לפי `DORONS_RAVE_UI_REDESIGN_IMPLEMENTATION_BRIEF.md`. **שלושה קבצים בלבד:**
`Index.html`, `Stylesheet.html`, `JavaScript.html`. אפס שינוי ב-`.gs`, בסכימה, בלוגיקה או בנתונים.

### רשת ביטחון לפני שינוי
גיבוי ל-`.ui-backup/` (הפרויקט אינו git repo, ולכן העתקת קבצים היא נקודת השחזור), ו-`check-ui.js`
שנעל את הבסיס: **40 endpoints ו-81 פעולות משתמש**. הוא מורץ אחרי כל שינוי ומוכיח שכלום לא אבד.

### תיק האירוע — חמש קבוצות עבודה
מתחילים באירוע, לא במספרים: כותרת ← ספירה לאחור ← צ'יפים של מצב ← פעולות מהירות ← לטיפול עכשיו,
ואז חמש קבוצות עם כותרות-על: **הפקה · אנשים · כספים · תיעוד**.
עשרה אקורדיונים, כולם סגורים, אחד נפתח בכל פעם, והסיכום בכותרת גלוי גם כשסגור.
"הוצאות לפי תחום" חדש: ההוצאות מקובצות לפי קטגוריה ותת-קטגוריה עם ספירה וסכום — הקטגוריות
מתפקדות כלוח ההפקה, לא כטבלת הנהלת חשבונות.

### מסך הבית — לפי הסדר המחייב בבריף
לוגו ← ברכה ← לטיפול עכשיו ← KPI ← אירועים פעילים ← תשלומים קרובים ← תמונה פיננסית ←
פעולות מהירות ← ניהול ← היסטוריה.
**הוחזרו:** פאנל "תמונה פיננסית" (הוסר בטעות בשלב 1), ושש פעולות יצירה במקום ארבע
(נוספו הכנסה ותזכורת). שורת אירוע אחת ועשירה מחליפה את הפוסטר ואת השורה הקומפקטית.

### רשימת אירועים
סינון עתידיים / שהיו / הכל, חיפוש, מונה תוצאות, ו"אירוע חדש" גלוי.
`getEvents` לא מחזיר `is_upcoming`, ולכן הוא נגזר בצד הלקוח מהתאריך — בלי לגעת בשרת.

### קטגוריות
חיפוש שמתאים גם לשם קטגוריה וגם לתת-קטגוריה ומרחיב אוטומטית את מי שההתאמה בתוכו,
מונה "8 קטגוריות · 36 תתי-קטגוריות", **אחת פתוחה בכל פעם**, כפתור "פתח הכל",
ושורת תת-קטגוריה שכולה לחיצה — במקום אייקון עיפרון על כל שורה.

### טופס הוצאה — ארבעה אזורי עבודה
"מה מתחייבים" · "מצב כספי" · "מה כלול במחיר" · "פרטים נוספים".
**אף שדה לא הוסר.** אזור התשלומים עלה מ"פרטים נוספים" ל"מצב כספי", לצד הסטטוס והסכומים.
כפתור השמירה דביק מעל ה-notch.

### אמנים וספקים — מסך Relationship
מסך אדם חדש: Badge "עבדנו יחד N פעמים", סה"כ, ממוצע לאירוע, טווח מחירים, האירוע הקרוב,
וכל ההופעות הקודמות עם שעות ומשך.
**משתמש ב-`getArtistExpenseHistory` שהיה קיים בשרת מאז שלב 1 ומעולם לא נקרא מהממשק** —
יכולת שהייתה שם, בלי שורת `.gs` אחת חדשה.

### מערכת עיצוב ונגישות
`[hidden]` גובר על כל כלל class (באג אמיתי: אקורדיון "סגור" יכול היה להישאר פתוח) ·
`:focus-visible` לכל אלמנט אינטראקטיבי · `prefers-reduced-motion` · `.sr-only` לסטטוסים ·
`aria-label` לכל כפתור אייקון · `aria-expanded`/`aria-controls` לכל אקורדיון ·
כותרת Bottom Sheet דביקה · סמל המטבע לא יורד מתחת ל-14px.

### אימות
- `check-ui.js`: 40 endpoints ו-81 פעולות — **כולם קיימים**. JS מתפרש.
- שש סוויטות השרת עוברות. `find src -name "*.gs" -newer` מחזיר **ריק** — אף קובץ שרת לא נגע.
- בדפדפן: 12 מסכים + 8 Bottom Sheets, אפס שגיאות JS, ואפס מסך שגיאה.
- 320px / 360px / 375px / 390px: **בלי גלילה אופקית באף מסך**, אפס טקסט מתחת ל-14px,
  ואפס אזור מגע מתחת ל-44px.
- אקורדיון: בדיוק אחד פתוח. חיפוש קטגוריות: "הגברה" ← קטגוריה אחת, נפתחת אוטומטית.
- נבדקו: הכנסה, תזכורת, עריכת אירוע, תפריט פעולות, שכפול, חיפוש + מסננים, השוואה,
  אמצעי תשלום, ייבוא, ומסך האמן.

**תוקנו תוך כדי:** `summaryQuadHtml` נמחק בטעות עם בלוק הבית (מסך השגיאה של האפליקציה תפס אותו);
`timeRangeText` מחזיר HTML ועבר escape כפול בהיסטוריית האמן; "ב2 אירועים" ← "ב-2 אירועים".

- **נפרס @17** (באישור המשתמשת, 2026-09-17), אותו קישור.
  נקודת שחזור: `.ui-backup/` מכילה את שלושת קבצי הממשק לפני העיצוב המחדש, וגם את `check-ui.js`.

## 2026-09-17 — UX Refinement V2 (נפרס @18)
שכבת UI בלבד. `find src -name "*.gs" -newer` מחזיר **ריק**.

### מרכז הפעולות — הליבה של V2
ההבחנה שהניעה את השינוי: **"לטיפול עכשיו" ערבב שני סוגי פריטים שאסור לערבב.**
בלי לדעת מי יצר פריט ואיך סוגרים אותו, הבלוק מרגיש מת.

- **"דורש ממני פעולה"** — התראות מערכת. נוצרות לבד מנתון אמיתי ונעלמות לבד.
  **אין כפתור השלמה** (אומת: `alert-item .task-tick` = 0).
- **"המשימות שלי"** — מ-Notes. ארבע פעולות לכל שורה, כולן על endpoints קיימים:
  השלמה (`toggleNoteCompleted`) · דחייה למחר/+שבוע (`saveEntityNote` עם `reminder_date`) ·
  עריכה (`saveEntityNote` עם `note_id`) · מחיקה (`archiveEntityNote`).

**חוק אמינות:** כל התראה נגזרת משדה קיים — Payments.due_date · agreed_amount בלי Payment ·
manual_status · average_ticket_price · היעדר Income · break_even_remaining · event_date ·
artistLineup.overlaps_with. **אין ניחושים.**

**חפיפת אמנים** נוספה כהתראה — הנתון היה קיים (`overlaps_with`) ומעולם לא הוצג כפעולה.
לפי הכרעת המשתמשת היא מופיעה **רק במסך האירוע**, כי `getInitialData` לא נושא את הליינאפ.

### שאר V2
- **סדר הבית:** KPI ← אירועים פעילים ← מרכז הפעולות ← גישה מהירה ← תשלומים ← תמונה פיננסית ← ניהול ← היסטוריה.
- **KPI:** מספר שלילי מוצג כ**"הפסד צפוי 26,000 ₪"**, בלי מינוס.
- **Badges:** צומצמו לארבעה — ימים · נותר לתשלום · חסרים · נקודת איזון.
- **גישה מהירה 2x2:** ניווט בלבד (אירועים/אמנים/ספקים/קטגוריות). **כל פעולות היצירה עברו ל-FAB**,
  שעכשיו מציע שש: אירוע · הוצאה · הכנסה · תזכורת · ספק · אמן. אין כפילות ואף פעולה לא אבדה.
- **Accent:** חמישה גוונים על אייקונים, כותרות, Badges ופס עליון דק. בלי רקעים צבעוניים.
  **מבטל את החלטה 48** שאסרה שפת צבעים חדשה. סגול (`--violet`) הוא הטוקן היחיד החדש.
- **Cards:** פחות גבולות פנימיים, יותר ריווח.

### הפערים מהבריף הקודם — נסגרו
1. **48px:** שבעה כללים הועלו, ושלושה יעדים נוספים (`brand-mark`, `person-phone`, `cmp-check`)
   קיבלו אזור מגע שקוף ב-`::after` — הגובה החזותי נשמר.
   **אומת: אף אלמנט אינטראקטיבי מתחת ל-48px, ב-12 מסכים.**
2. `role="alert"` נוסף ל-`renderError`.
3. "הצג תוצאות" במסנני החיפוש עטוף ב-`.sheet-actions` דביק.
4. **עלות לשעה** במסך האמן — "4,000 ₪ לשעה", מחושבת מהנתונים הקיימים.
5. **מסך ספק מלא** — אירועים, תחומי הפקה, סכומים ויתרות, מ-`searchExpenses({vendor_id})`.
6. "ייבוא ספקים ואמנים" נוסף לרשימת ההגדרות.

### אימות
- `check-ui.js`: **40 endpoints ו-81 פעולות** — כולם. שש סוויטות השרת עוברות.
- 12 מסכים, אפס שגיאות JS, אפס מסך שגיאה.
- 320/360/375/390: בלי גלילה אופקית, אפס טקסט מתחת ל-14px, אפס מגע מתחת ל-48px.
- **ארבע פעולות המשימה נבדקו מקצה לקצה מול הגיליון:** דחייה כתבה 2026-09-18,
  עריכה נפתחה מלאה, והשלמה הורידה את המשימה מהרשימה.

**תוקנו תוך כדי:** `quickAccessHtml` נמחק בטעות עם בלוק הפעולות (מסך השגיאה תפס אותו) ·
`money()` מחזיר HTML ועבר escape כפול בהיסטוריית הספק · "לDORON'S DJ" ← "ל-DORON'S DJ"
(תיקון תצוגה, בלי לגעת בכותרות שהשרת בונה).

- **נפרס @18** (באישור המשתמשת, 2026-09-17), אותו קישור. נקודת שחזור: `.ui-backup/`.

## 2026-09-17 — אריחי הניהול חוזרים צבעוניים (נפרס @19)
המשתמשת הראתה צילום של הגרסה הישנה: "מבחינת צבעים ועיצוב ומבנה שיהיה כמו זה...
אני רוצה יותר צבע. וכתב לבן."

- **גריד אחד של אריחים צבעוניים** במקום "גישה מהירה" + רשימת ניהול מקופלת.
  `manageTilesHtml` וכל ה-CSS של האריחים **לא נמחקו מעולם** — רק לא היו בשימוש, אז ההחזרה הייתה מיידית.
- **יותר צבע:** עוצמת הגרדיאנט עלתה מ-0.24 ל-0.42, וכל אריח קיבל גוון — כולל `t-sand` שלא היה צבוע.
  נוסף `t-violet` לאמנים.
- **כתב לבן:** `.tile-label` ו-`.tile-count` הם `#fff` מפורש. המונה הוגדל ל-`--display-md`.
- עשרה אריחים: אירועים · אמנים · ספקים · קטגוריות · תשלומים · אמצעי תשלום · חיפוש ·
  השוואה · Google Sheets · ייבוא. כולם עם מונה היכן שיש.
- **תוקן:** אריח הייבוא שאל את המונה של האמנים (חלק מ-`key: 'artists'`) — נוסף אייקון `import` ייעודי.

**מבטל את החלטה 61** ("אזור הניהול הוא תפריט ולכן רשימה מקופלת"), שנקבעה לפי פידבק היועץ.
בקשת המשתמשת גוברת.

- **אימות:** 40 endpoints · 81 פעולות · שש סוויטות · אף `.gs` לא נגע ·
  12 מסכים בלי שגיאות · אפס מגע מתחת ל-48px · בלי גלילה אופקית ב-320 ו-375 ·
  לחיצה על אריח מנווטת נכון.
- **נפרס @19** באישור המשתמשת.

## 2026-09-18 — V5: שפת בלוקים אחת לכל המערכת (נפרס @20)
בקשת המשתמשת, עם צילומי מסך: "דף הבית — ריבועים צבעוניים, מלל בצבע לבן, נוח לתפעול.
לעומת זאת דף אירוע נראה לא מותאם לשימוש... שהתוכן גם יהיה בצבע לבן. בצבעים בולטים.
שהבלוקים יהיו נגישים ולא כל דבר צריכה לגולל... בכל אירוע, למעלה יש תחשיבים נכונים, פעולות מהירות."

**שכבת UI בלבד.** `find src -name "*.gs" -newermt 2026-09-18` מחזיר **ריק**, ו-`Index.html` לא נגע.

### מה הייתה הבעיה
מסך האירוע היה עשרה פסים כמעט זהים בגוונים חיוורים: אותו גובה, אותו משקל, כתב אפור,
ושום מספר לא נראה בלי לפתוח משהו. הוא לא נבנה בשפה של הבית — הוא נבנה בשפה של רשימה.

### שפת הבלוקים (`Stylesheet.html`)
- **הגוונים שוחררו מ-`.tile`.** `.t-clay/.t-sage/.t-sky/.t-sand/.t-ember/.t-violet` הם עכשיו
  מחלקות עצמאיות שמגדירות `--tile-a/-b/-line/-accent`. אריח, בלוק, שורת אירוע וכרטיס אדם
  שואבים מאותם ארבעה טוקנים — גוון חדש הוא שורה אחת.
- **בלוק = פס כותרת צבוע מעל גוף כהה.** הצבע חי בכותרת (אייקון בתיבה + כותרת לבנה + ערך לבן),
  והגוף נשאר `--ink-raised` כדי שהתוכן הלבן ייקרא. נדרשה סלקטיביות של שתי מחלקות
  כדי לגבור על כללי ה-tint הישנים.
- **`--muted` הועלה** מ-`#c2baab` ל-`#d3ccbf` ו-`--faint` ל-`#b7b0a2`, ונוסף `--white`.
  כל מה שהוא **מידע** (שם הוצאה, שם אדם, כותרת התראה, סכום) הוא לבן; רק הקשר נשאר משני.
- **רכיבים חדשים:** `.stat-tile` (אריח חישוב), `.act-tile` (פעולה מהירה), `.event-hero`, `.ring`.

### מסך האירוע
1. **בלוק גיבור צבעוני** — שם, תאריך, ומתחתיו **טבעת מוכנות** (conic-gradient, אחוז אמיתי
   מ-`EVENT_REQUIREMENTS`) לצד הספירה לאחור. **הגוון נושא מידע:** כתום = תוך שבוע ·
   כחול = עתידי · חול = כבר היה. אותו כלל בדיוק חל על שורת אירוע ברשימה.
2. **ארבעה אריחי חישוב מיד מתחת** — הוצאות · נותר לתשלום · הכנסות · מאזן.
   כל אריח נושא **שני** מספרים: הראשי, ומתחתיו ההקשר (מתוכנן / שולם / כרטיסים / רווח צפוי).
   כך **שמונה נתונים כספיים נקראים בלי לפתוח אף בלוק ובלי לגלול** — זה המענה ל"למעלה תחשיבים נכונים".
3. **לחיצה על אריח פותחת את הבלוק שממנו המספר מגיע** (`openEventSection`) וגוללת אליו.
   זה המענה ל"ולא כל דבר צריכה לגולל": המספר למעלה הוא גם הדרך לפירוט שלו.
4. **פעולות מהירות כאריחים צבעוניים עם אייקון** (הוצאה חמר · הכנסה מרווה · תזכורת גחלת),
   במקום שלוש קופסאות אפורות.
5. **לכל בלוק אייקון וגוון משלו** — אמנים סגול, ספקים כחול, תשלומים גחלת, הכנסות מרווה,
   הוצאות חמר, כרטיסים כחול, הערות סגול.
6. **צ'יפים נוקו מכפילויות** — "4 מתוך 11 סגורים" הופיע גם בטבעת וגם בצ'יפ, ו"32,500 לתשלום"
   גם באריח וגם בצ'יפ. נשארו רק החריגות (חסרים / בתהליך / הכל סגור).

### אותה שפה בכל המסכים (לא לפי מסך)
- **`kpiGridHtml` בבית משתמש עכשיו באותו `.stat-tile` בדיוק** של ראש מסך האירוע.
  מספר ראשי נראה זהה בכל המערכת ולא נבנה מחדש לכל מסך.
- **`panelHtml` ו-`collapsiblePanelHtml` קיבלו `sectionKey`** — אותו פס כותרת עם אייקון
  לבלוק קבוע ולאקורדיון. כל מסך שמשתמש בפאנל קיבל את השפה החדשה בלי שורת קוד משלו.
- **שורת אירוע, כרטיס אדם ושורת תשלום הפכו לבלוקים צבעוניים** בכתב לבן —
  אירועים, אמנים (סגול), ספקים (כחול), תשלומים (גחלת, ובאיחור חמר).

### מה נבדק (`test-eventblocks.js`, 40 בדיקות חדשות)
סוויטה חדשה שבודקת **כל בלוק במסך האירוע מול השורות הגולמיות** ולא מול סיכום אחר:
- מוסכם = סכום `agreed_amount` · שולם = סכום התשלומים ששולמו · נותר = מוסכם פחות שולם ·
  הכנסות = סכום שורות ההכנסה · מאזן = הכנסות פחות מוסכם · מתוכנן נופל ל"סוכם" לכל הוצאה בנפרד.
- "הוצאות לפי תחום" מכסה כל שקל, ואין הוצאה בלי קטגוריה לקבץ תחתיה.
- סכום תתי-הקטגוריות = סכום הקטגוריה, וסכום הקטגוריות = סה"כ.
- כל תשלום של האירוע מופיע בפאנל התשלומים, והמשולמים שבהם מסתכמים ל"שולם".
- **פער מתועד:** "ספקים ואנשי קשר" נגזר מההוצאות, ולכן הוצאה בלי ספק ובלי אמן לא מופיעה בו.
  זה נכון — זה בלוק של אנשים, לא של כסף — ונעול בבדיקה כדי שלא ייקרא בטעות כחוסר.
- נקודת איזון = `ceil((מתוכנן − הכנסות שאינן כרטיסים) / מחיר)`, ולעולם לא שלילית.
- "לא צריך" **לא משנה אף סכום**, מקטין את המכנה, ומעלה את אחוז המוכנות.
- ארכוב הערה והוצאה: יורדים מהמסך, **והשורה נשארת בגיליון**. אפס מחיקה.
- עריכה מתגלגלת לסיכומים, לחלוקה לפי קטגוריה ולחלוקה לפי ספק.
- שש קריאות שגויות (שם ריק, בלי אירוע, בלי תאריך, בלי שם, תשלום בלי הוצאה, מפתח לא מוכר)
  **נדחות, ולא כותבות כלום**.

### מה נבדק בדפדפן (על השרת האמיתי מול Sheets מדומה)
- **12 מסכים, אפס שגיאות JS, אפס מסך שגיאה.** 320px ו-390px: בלי גלילה אופקית,
  אפס טקסט מתחת ל-14px, **אפס אזור מגע מתחת ל-48px**.
- **אריח → בלוק:** לחיצה על "נותר לתשלום" פתחה את פאנל התשלומים; לחיצה על "הוצאות"
  פתחה את ההוצאות **וסגרה את הקודם** — בדיוק אחד פתוח, `aria-expanded` מתעדכן.
- **סכומי "הוצאות לפי תחום" נספרו בדפדפן: 18,000 + 12,000 + 8,500 + 3,000 = 41,500** —
  זהה למספר בכותרת הבלוק ולאריח החישוב.
- **מחזור עריכה מלא:** שינוי סכום מוסכם 8,000 ← 8,500 החזיר "נשמר בהצלחה",
  והאריחים התעדכנו על המסך: הוצאות 41,500 ← 42,000, נותר 32,500 ← 33,000, מאזן 42,500 ← 42,000.
- **מצבי שגיאה:** שם הוצאה ריק ← "שם ההוצאה הוא שדה חובה", הגיליון לא נגע, והטופס נשאר פתוח.
  אותו דבר בהכנסה.
- **הוספה:** הכנסה 3,000 ← אריח ההכנסות 84,000 ← 87,000. תזכורת ← כותרת ההערות 2 ← 3.
- **"לא צריך":** 11 ← 10 במכנה, טבעת 36% ← 40%, חסרים 6 ← 5, **ואף סכום לא זז**.

### תוקנו תוך כדי
- `SECTION_ICONS` הוגדר לפני `var ICONS` ושבר את כל קובץ ה-JS בזמן הטעינה
  (`check-ui.js` בודק תחביר ולכן לא תפס את זה — **רק הקונסול בדפדפן תפס**). הועבר אחרי `ICONS`.
- `.panel-title:has(.panel-icon)` הוחלף במחלקה `has-icon` — בלי תלות בסלקטור מודרני.
- **אזור המגע של `.task-tick` היה 44px ולא 48** כפי שתועד ב-V2: ה-`::after` ממוקם מול תיבת
  ה-padding (26px פחות 2px גבול מכל צד), ולכן `inset: -11px` נתן 44. תוקן ל-`-13px` ונמדד מחדש.
- **"טווח מחירים" במסך האמן נחתך** בחצי רוחב. נוסף `overflow-wrap: anywhere` לערכי `.stat-box`.
- `.card-list .event-row` החזיק `background: var(--ink-raised)` בסלקטיביות גבוהה יותר
  והשאיר את שורות רשימת האירועים אפורות. הגרדיאנט הוסף לאותו כלל.

### פער שנשאר פתוח — דורש החלטה
**`getEvents` לא מחזיר `readiness` ו-`pnl`.** לכן אותה שורת אירוע מציגה בבית
"36% מוכן · 6 חסרים · כרטיסים לאיזון" ובמסך "אירועים" רק ימים ויתרה. זה לא באג בעיצוב —
זה חוסר בנתון. הסגירה היא שורה אחת ב-`getEvents` שקוראת ל-`summarizeEventReadiness_`
(כבר קיימת ומשמשת את `getInitialData`). **לא בוצע**, כי החלטה 62 נועלת את השינוי
ל-`Index/Stylesheet/JavaScript` בלבד — צריך אישור לפני נגיעה ב-`.gs`.

- **נקודת שחזור:** `.ui-backup/v19-before-v5/` — שלושת קבצי הממשק כפי שהיו ב-@19.
  `.ui-backup/` עצמה נשארת בסיס ההשוואה של `check-ui.js` ולא נגעה.
- **נפרס @20** (באישור המשתמשת, 2026-09-19), אותו קישור: https://tinyurl.com/doronsrave

## 2026-09-20 — הורדת "דורש טיפול" + כרטיס אירוע חדש (נפרס @21)

### א. עיצוב בהיר — נבנה, ובוטל
הגיע `DORONS_RAVE_LIGHT_UI_PRODUCER_EXPERIENCE_CLAUDE_BRIEF_2026-09-20.md`: מעבר מלא
ל-light mode, 7 מנות. לפי §22 הוגשה תוכנית לפני קוד, כולל מיפוי הסתירות להחלטות הנעולות.
**מנות 1–2 בוצעו במלואן:**
- מערכת טוקנים בהירה. היפוך בלוק ה-`:root` הספיק לרוב המערכת, כי הכל כבר היה מונע-טוקנים.
  השמות הישנים נשמרו כ-aliases כדי ש-93KB של כללים קיימים לא יישברו.
- **הפלטה שהבריף הציע נכשלה בדרישת הניגודיות של הבריף עצמו (§16).** `--brand #b28a46`
  נמדד 2.91:1. נבנתה מערכת דו-שכבתית: `-accent` לקישוט, `-ink` לטקסט, כל גוון חושב
  מול הרקע הרך שלו (>= 4.58:1).
- **הלוגו** הוא משיכות מכחול בקרם על שקוף — הוא נבנה להיות בהיר על כהה, ועל אופ-וייט
  הוא נעלם. הפתרון: לוח מותג כהה מתחתיו.
- נבנה `audit.js` — בודק בזמן ריצה את **הצבע שהפיקסל באמת מקבל** (עם שקיפויות),
  אזורי מגע בפיקסלי layout, גודל טקסט וגלישה אופקית, על פני 12 מסכים.
  תפס 4 כשלי ניגודיות שה-grep לא יכול היה לתפוס.
- תוצאה: נקי ב-320/360/375/390/768/1024, אפס שגיאות JS.

**המשתמשת ביקשה לבטל: "לא לא. בטל את השינויים. לא מעוניינת."**
שלושת קבצי הממשק הוחזרו מ-`.ui-backup/v20-before-light/`, מאומת ב-md5. אפס שאריות.

### ב. "דורש טיפול" יורד — מה שהיא כן ביקשה
לשונה: *"אלו משימות שהבינה שמה לי בלי לשאול אותי. לא מעוניינת."*

- **בבית נשאר בלוק אחד: "התזכורות שלי"**, מסונן `kind === 'reminder'` — רק שורות
  שהיא הקלידה ב-Notes. שלוש השורות שהמערכת הסיקה ("לשלם ל-Shaman", "לסגור את
  פרוז'קטורים", "לקבוע מועד תשלום") ירדו. **אומת בדפדפן: 0 שורות מומצאות בבית.**
- שורת הפתיחה כבר לא סופרת "7 פעולות פתוחות".
- אריח KPI "לטיפול" ← "תשלומים קרובים".
- **מסך האירוע באותו כלל**, עם חריג אחד: **חפיפת שעות בין אמנים נשארה**, תחת
  "סתירה בנתונים" — זו סתירה אמיתית בנתונים שהיא הזינה, לא משימה שהומצאה.
- **אפס מחיקת יכולת:** המסך המלא עבר להגדרות בשם "התראות מערכת".

### ג. כרטיס אירוע חדש — לפי פריסה שהיא כתבה
היא נתנה מפרט מדויק ונימוק: *"העין קוראת את זה הרבה יותר מהר... הבעיה היא שכולם
מתחרים אחד בשני."* ערימה אנכית במקום שתי עמודות מתחרות:
שם · ◎ מיקום · תאריך · עוד X ימים · רווח צפוי · כרטיסים שנמכרו · אמנים · לתשלום.

**שינוי שרת שאושר במפורש:** `getEvents` ו-`getInitialData` מצרפים `pnl` ו-`artists_count`
לכל אירוע דרך `eventCardMetrics_`. קריאה בלבד, בזיכרון, אפס עמודות, אפס חישוב חדש.

**שתי סטיות מהמפרט שלה, בכוונה:** "150 כרטיסים" ← "320 מתוך 600 כרטיסים שנמכרו"
(כי "כרטיסים" לבד לא אומר אם זה כסף או כמות), ו-📍 ← אייקון וקטורי מאותה משפחה.

**`test-upgrade.js` נכשל ותוקן:** הייתה בדיקה "הבית לא מחשב P&L לשאר האירועים" —
כלל ביצועים שהכרטיס מבטל. הבדיקה עודכנה לשמור על מה שבאמת היה בסיכון: שהחישוב
נשאר בזיכרון ואינו מוסיף קריאות ל-Sheets.

### ד. אימות החישובים (לבקשתה)
`test-eventblocks.js` — 44 בדיקות, כל בלוק מול השורות הגולמיות. **הכל עובר.**
מוסכם/שולם/נותר/הכנסות/מאזן · "מתוכנן" נופל ל"סוכם" לכל הוצאה בנפרד ·
סכום תתי-הקטגוריות = הקטגוריה · נקודת איזון · "לא צריך" לא מזיז סכום ·
ארכוב משאיר את השורה בגיליון · 6 קריאות שגויות נדחות בלי לכתוב.

### ה. ממצאים שעלו בבדיקה
1. **טעות שלי שתוקנה בפני המשתמשת:** אמרתי שאמנים עלולים להופיע כ-0 בכרטיס.
   באירוע הבדיקה שלי לא הזנתי שעות, ומזה הסקתי מסקנה רחבה מדי. **עם שעות מלאות
   האמן נכנס לליינאפ כרגיל.** מה שכן נכון: המונה חייב להיספר מההוצאות ולא מהליינאפ.
2. **באג 24 השעות** — `22:00–22:00` נותן 1440 דקות. לא תוקן, ממתין לאישור.
3. **אמנים מופיעים פעמיים** — `peoplePanelBodyHtml` בונה "ספקים ואנשי קשר" מ-
   `artist_name || vendor_name`, ולכן אמן מופיע גם שם. לא תוקן.
4. **הכסף מפוזר** — 4 אריחים + 6 בלוקים, עשרה מקומות לאותו נושא. לא טופל.
5. **"מצב ההפקה"** — ראו ההחלטה הפתוחה ב-HANDOFF.

### ו. אמון: נתוני הבדיקה
המשתמשת שאלה *"מה זה פסטיבל חורף.. המצאת נתונים?"* אחרי שפתחה קובץ תצוגה מקדימה
ששלחתי. **התשובה: נתוני בדיקה בלבד.** `fake-gas.js` מחליף את `SpreadsheetApp`
באובייקט בזיכרון — `openById()` מחזיר גיליון מדומה ואין שום קריאת רשת. השמות
כתובים ב-`build_real.py`. **בגיליון שלה לא נכתבה שורה.**
**מסקנה לסשנים הבאים: לעולם לא לשלוח קובץ תצוגה בלי לכתוב במפורש שהנתונים מדומים.**

### אימות סופי
שבע סוויטות שרת · `check-ui.js`: 41 endpoints, 81 פעולות · אפס שגיאות JS ·
אפס ניגודיות/מגע/גלישה ב-390 · Index.html לא נגע.
שינוי מול @20: 37 שורות CSS, 158 שורות JS, ושני שינויי שרת מאושרים.

- **נפרס @21** (באישור המשתמשת, 2026-09-20), אותו קישור.
  נקודת שחזור: `.ui-backup/v20-before-light/`.

## 2026-09-20b — מצב ההפקה עובר למשתמשת, ושני באגים (נפרס @22)

המשתמשת אישרה את כל ארבעת הפתוחים. שלושה בוצעו; היררכיית מסך האירוע נשארה פתוחה.

### 1. באג משך ההופעה של 24 שעות
`calculatePerformanceDuration_` הוסיף 24 שעות על כל הפרש `<= 0`, ולכן `22:00–22:00`
נתן 1440 דקות ו"עלות לשעה" של הסכום חלקי 24. תוקן בשני מישורים:
חציית חצות חלה על הפרש **שלילי** בלבד, והשמירה נחסמת עם הודעה ברורה.
נבדק: 23:30–01:30 עדיין 120 דקות; 22:00–22:00 נדחה.

### 2. אמן שהופיע פעמיים
`peoplePanelBodyHtml` נבנה מ-`artist_name || vendor_name`. תיקון נאיבי (לסנן אמנים)
היה גורם לאמן **בלי שעות** להיעלם משני הבלוקים, כי הליינאפ כולל רק משובצים.
לכן: הספקים מסננים אמנים, **והליינאפ מציג את כולם** — משובצים לפי רצף הלילה,
ואחריהם "אמן אחד בלי שעות". אומת בדפדפן: ספקים = 2 ספקים אמיתיים, ליינאפ = 3 אמנים.

### 3. מצב ההפקה — הרעיון של המשתמשת
לשונה: *"אמור להיות כל אותם דברים שעשיתי או לא עשיתי. אולי יהיה נכון לשים את
הקטגוריות ואז אני אסמן אם צריך או לא צריך."*

**הממצא שהצדיק את זה:** כל 11 השלבים הקבועים כבר ישבו בטקסונומיה שלה, אבל
**25 תתי-קטגוריות נשארו בחוץ** — גדרות · אוהלים · שילוט · הסעות · פחי אשפה · ציליה ·
צלם · פרסום · צוות הקמה/פירוק. אירוע בלי הסעות הראה "36% מוכן" ולא רמז במילה.

- **סכימה:** `Events.required_items` בסוף הטבלה. `SCHEMA_VERSION` הועלה כדי שהבדיקה
  תרוץ שוב אצל מי שכבר שודרג. `planSchemaUpgrade_` הגנרי מוסיף את העמודה לבד.
- **התאמה לפי מזהה ולא לפי שם** — יכולת חדשה שלא הייתה: שינוי שם של תת-קטגוריה
  היה שובר את הרשימה הקבועה. נעול בבדיקה.
- **אירוע שלא הוגדר לא מציג אחוז, גם לא 0%.** `configured: false`.
- **מסך בחירה** מקובץ לפי קטגוריה, כלום לא מסומן מראש. קטגוריה בלי תתי-קטגוריות
  (כמו "אמנים") נבחרת בעצמה. שמירה אחת ב-`setEventRequiredItems`.
- **"סגור גם (לא סומן כנדרש)"** — הוצאה בתחום שלא נבחר מוצגת כהקשר, לא כחוסר.

**אומת בדפדפן מקצה לקצה:** סימון 4 רכיבים ← רשימה של 4, טבעת 75%, "3 מתוך 4",
"אבטחה" חסר, ו"סגור גם" הציג את השאר.

### הבדיקות נכתבו מחדש, לא הותאמו
`test-stage2.js` נעל את המודל הישן על 11 שלבים. הוא נכתב מחדש (25 בדיקות) ונועל עכשיו
את הכלל ההפוך: **המערכת לא מחליטה מה אירוע צריך.** כולל: בלי בחירה אין רשימה ואין אחוז ·
שינוי שם לא שובר · מפתח לא קיים נדחה · הבחירה לא מזיזה סכום · נשמרת בתא אחד בלי טבלה חדשה.
גם `test-stage1.js` ו-`test-eventblocks.js` עודכנו. **שבע סוויטות עוברות.**

### CALCULATIONS.md — חדש
לבקשת המשתמשת: מסמך ייחוס של **כל** חישוב במערכת, עם הנוסחה והמיקום בקוד —
יסודות, הוצאה בודדת, סיכומי אירוע, P&L, מצב ההפקה, לוח אמנים, חלוקות, המספרים בבית,
חלונות זמן, ו**פערים ידועים**. חולץ מהקוד ולא מהזיכרון.

### אימות
שבע סוויטות · `check-ui.js`: 42 endpoints (נוסף אחד), 81 פעולות · אפס שגיאות JS ·
אפס ניגודיות/מגע/גלישה · `Index.html` לא נגע.

- **נפרס @22** (באישור המשתמשת, 2026-09-20).
  **שים לב:** אחרי הפריסה אירועים קיימים מציגים "לא הוגדר" במקום אחוז, עד שהמשתמשת
  תסמן לכל אירוע מה הוא צריך. זו הכוונה.

## 2026-09-20c — סבבי תמחור, שמות, וסגירת פער בחלוקה (נפרס @23)

### סבבי תמחור לכרטיסים
שאלת המשתמשת: *"אני יכולה להגדיר מספר מחירים לכרטיסים?"* — ואז:
*"אני רוצה שתהייה לי אופציה לפתוח עוד סבב — סבב מאחרים או סבב VIP."*

**הבדיקה שקדמה לתשובה הפרידה בין שני דברים:**
- **מכירות בפועל** — כבר תמכו בכמה מחירים. כל שורת הכנסה עם כמות ומחיר משלה;
  אומת: 100×200 + 150×250 = 250 כרטיסים, 57,500 ₪, ממוצע 230.
- **תחזית ונקודת איזון** — מחיר **אחד** בלבד. זה מה שנבנה עכשיו.

`Events.ticket_tiers`, JSON לפי סדר המכירה. **נקודת האיזון נמכרת לפי הסדר**
(`ticketsToCover_`) ולא לפי ממוצע: ממלאים את הסבב הזול, וכשהוא נגמר עוברים ליקר.
אומת: עלות 50,000 עם 100×150 / 150×200 / 50×400 → 263 כרטיסים (100 + 150 + 13).

**מה שלא מוצג כ-0:** אם כל הסבבים יחד לא מכסים את העלות, `break_even_tickets = null`
ו-`break_even_unreachable = true`, והממשק אומר את זה במילים.

**תאימות לאחור מלאה:** בלי סבבים, `average_ticket_price` + `expected_ticket_count`
עובדים בדיוק כמו קודם. נעול ב-`test-tiers.js` (18 בדיקות).

**מלכודת שתועדה:** `average_ticket_price` גובר על הממוצע בפועל. בבדיקה — מכירות
בממוצע 230 מול שדה שמכיל 200 הזיזו את נקודת האיזון מ-218 ל-250. זו החלטה 24
ולא באג, אבל מי שמוכר בכמה מחירים צריך לדעת: או סבבים, או להשאיר את השדה ריק.

### שמות: "למערכת" מול "לאירוע"
המשתמשת הגדירה: *"הוסף אמנים למערכת = הוספה של רשומה בלבד. הוספת אמן לאירוע =
הוספה של הוצאה."* הבלבול היה אמיתי — הכפתור אמר "הוסף אמן לאירוע" ונפתח חלון
בשם "הוספת הוצאה".

`openExpenseSheet` קיבל פרמטר `intent`. כשנכנסים דרך הכפתור: הכותרת "הוספת אמן
לאירוע", **וקטגוריית "אמנים" כבר מסומנת** עם שדות השעות פתוחים. אותו כלל הוחל על ספקים.
אומת בדפדפן: טופס "למערכת" — אין בו שדה עלות; טופס "לאירוע" — קטגוריה ממולאת, שעות פתוחות.

### החלוקה לפי ספק — הפער נסגר
המשתמשת קראה את סעיף "פערים ידועים" ב-`CALCULATIONS.md` ואמרה "אז בוא נתקן".
הוצאה בלי ספק נאספת ל-`NO_VENDOR_LABEL` ("ללא ספק") במקום להישמט.
**החלוקה מסתכמת לסה"כ.** הבלוק "ספקים ואנשי קשר" לא השתנה — שם אמן באמת לא צריך
להופיע. המסמך עודכן כדי שההבחנה תהיה מפורשת.

### תחזוקת בדיקות
`test-stage2.js` נעל ש-`required_items` היא העמודה האחרונה, ו-`ticket_tiers` שברה את זה.
הבדיקה הוכללה: מה שחשוב הוא שהעמודה נוספה **אחרי** כל מה שהיה, ושהעמודות הקודמות
לא זזו — לא שהיא האחרונה. מנות הבאות יוסיפו אחריה.

### אימות
**שמונה סוויטות** (נוספה `test-tiers.js`) · `check-ui.js`: 43 endpoints, 81 פעולות ·
אפס שגיאות JS · אפס ניגודיות/מגע/גלישה · `Index.html` לא נגע.

- **נפרס @23** (באישור המשתמשת, 2026-09-20).


## 2026-09-22d — Design System v2 "Executive Luxury Refresh" (לא נדחף; הוחלף ב-22e)

**מקור:** בקשת המשתמשת, מסמך "Design System Update v2". Refinement בלבד — בלי לשנות שפה,
ניווט, Layout או רכיבים. ההחלטות נעולות ב-HANDOFF §0א.

### מה השתנה
- `tokens.css`: ערכי v2 (טקסט, גבול, Warning, Secondary חדש), צל אחד, Radius 12, תנועה 200ms ease.
- `Card.tsx`: `IconDisc` (אייקון בעיגול בגוון) + `KpiCard` בסדר אייקון → מספר → Label → שורה;
  Badge בגובה 28px; Progress 8px.
- `Icon.tsx`: קו 1.75px absolute, גדלים `xs`/`sm` בלבד.
- `QuickAction`: צבע לכל קיצור (בית + ניהול, אותו צבע לאותו יעד).
- `EventCard`: Banner 80px (`src/assets/event-banner.webp`, 1.3KB, נגזר מ-`brand/link-preview`).
- `layout.tsx`: `CollapsibleSections` — בתוכו כל `Section` הוא Accordion סגור. מסך האירוע
  עוטף בו את כל הסקשנים; ה-KPI (`EventKpis`) מחוץ לו.
- שעות אירוע: מיגרציה `20260922110000_event_times.sql` (הוחלה על `fvttcoavqmvzmqqgkmta`),
  טופס + תצוגה ב-Header של "פרטי אירוע". `save_event` כותב שעות רק אם המפתח נשלח,
  כך שגרסת הלקוח שבאוויר לא מוחקת אותן.
- "תחזית כרטיסים" בטופס האירוע נפתחת רק בלחיצה (גם בעריכה).

### אימות
- DB: טרנזקציה שבוטלה — שמירה 22:00–07:00, שמירה בלי מפתחות לא מוחקת, זהות חסומה. הרשאות anon ללא שינוי.
- `tsc` נקי · ESLint נקי · 58 בדיקות (3 חדשות לשעות) · build עובר.
- Harness (נתוני דוגמה): בית, אירוע, טופס; 320px בלי גלישה ב-9 מסכים עם כל הסקשנים פתוחים.


## 2026-09-22e — יישור ל-Design System Canon V3 + UX Addendum (לא נדחף)

**מקור:** חבילת `DORONS_RAVE_DESIGN_UX_UPDATE_PACKAGE_2026-09-22.zip` (קנון) + פרומפט "Executive Luxury
Refinement" (הנחיה). V3 גובר על v2 — פירוט ב-HANDOFF §0א.

### מה השתנה מול 22d
- טוקנים חזרו לפלטת V3: טקסט `#1F2937`, גבול `#E5E7EB`, אזהרה `#CA8A04`, רך כתום `#FFF4E6`,
  טורקיז `--color-teal`; הכחול הוסר (`--color-info` = טורקיז). רדיוס 12/16/24, תנועה 120/180/240.
- `IconTile` (ריבוע מעוגל) במקום עיגול; KPI בסדר Tile → Label → Value; ערך KPI לא נשבר ומתכווץ לפי רוחב הכרטיס.
- כפתורים: ghost = Tertiary טורקיז; "העבר לארכיון" (7 מקומות) = Danger Soft.
- טיפוגרפיה: כותרות 700 בטקסט חזק; כותרת עמוד גדולה יותר; ריווח בין אזורים 32/40/48.
- כרטיס אירוע: Banner 72px, צל, כותרת 20px.
- מסך אירוע לפי Addendum §6: Hero + תמונה פיננסית + מוכנות גלויים; 6 סקשנים ב-Accordion עם
  אייקון, "(N)" וסיכום; הפעולה מחוץ לכפתור הפתיחה.
- שעות: מיגרציה `20260922120000_event_times_canon_names.sql` (הוחלה) — שמות קנוניים, הוסרה חסימת
  התחלה==סיום, views נבנו מחדש עם אותן הרשאות. `eventDurationMinutes` (helper יחיד), `lineupSpan`.
- סימן המותג בכותרת הבית.

### אימות
- DB בטרנזקציה שבוטלה: שמירה/טעינה 22:00–07:00, לקוח בלי מפתחות לא מוחק, זהות נשמרת, ניקוי ל-null.
  הרשאות views: authenticated SELECT בלבד.
- tsc · ESLint · 61 בדיקות · build — עוברים.


## 2026-09-22f — החלטות המשתמשת + ליטוש אחרון, נדחף

**החלטות (המשתמשת, 2026-09-22):** אין כחול — ספקים ניטרלי · התחלה == סיום חסום · סימן המותג נשאר ·
Push כולל `vercel.json`.

- מיגרציה `20260922130000_event_times_equal_blocked.sql` (הוחלה): check בטבלה + `event_hours_equal` ב-`save_event`;
  refine בטופס על שדה הסיום. נבדק בטרנזקציה שבוטלה: שמירה חסומה, UPDATE ישיר נחסם ב-check.
- ליטוש: `EventBanner` משותף לכרטיס ול-Hero; Hero עם עובדות תאריך/שעות/מקום; KPI — Label שקט, ערך
  30/36px, שורה משנית מופרדת בקו; גישה מהירה — רקע Accent Soft לכל אריח; כרטיס אירוע — הרמה עדינה
  ב-hover ותחתית עובדות מובחנת; Accordion/Sheet/Dialog — 200ms (מכבד reduced-motion).
- `web/vercel.json`: SPA rewrite + `sw.js` no-cache.
- אימות: tsc · ESLint · 61 בדיקות · build · 9 מסכים × 7 רוחבים (320–1440) בלי גלישה.
